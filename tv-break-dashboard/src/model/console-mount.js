// Deprecated compatibility entry point. The application no longer imports
// this side-effect module; ModelConsole is integrated into the canonical shell.
import { mountModelConsole } from './console-bridge.jsx';

mountModelConsole();
