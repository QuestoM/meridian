# Leave unset (or empty) in local dev so the SPA talks to the same origin
# (:3000) and Vite proxies /api → the Kairos API. Session cookies then stay
# first-party. Only set an absolute URL when the API is on a different host.
# VITE_KAIROS_API_URL=
