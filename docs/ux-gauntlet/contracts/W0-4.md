# W0-4 contract: the wall and the words

Five cross-cutting rules, each one importable module, each adopted in one line.
Published and frozen under section 8.8. Nothing in this piece changes what a
person sees or a number anyone reads; the modules exist and are tested, and the
routes that will use them belong to their owners.

## What this piece publishes and freezes

| Path | What it is | Owner after wave zero |
|---|---|---|
| `kairos_api/affiliation_wall.py` | The wall, `can_edit`, session identity | frozen |
| `kairos_api/channel_scope.py` | The competitor boundary | frozen |
| `tv-break-dashboard/src/vocabulary.js` | Every label, both languages | frozen |
| `tv-break-dashboard/src/session.js` | Who am I, what may I do, where do I land | frozen |
| `kairos_api/guardrail_store.py` | The regulatory limits, with a date and a record | P5 |
| `kairos_api/model_activation.py` | The audience model activation switch | P5 |
| `data/regulatory_guardrails.json` | The limits on disk | P5 |

Files edited in place, all on the W0-4 row: `kairos_api/auth.py`,
`kairos_api/auth_store.py`. Not edited: `kairos_api/events_access.py`,
`kairos_api/core.py`, `kairos_api/settings_api.py`, for the reasons in the last
section.

**One new endpoint**, `PUT /api/auth/job`. It is the only path this piece adds,
so the OpenAPI baseline of 90 paths, 113 operations and 56 writes becomes 91,
114 and 57 once wave zero settles. Measured against the captured baseline while
W0-1's router split and W0-3's identity work were in flight: this piece added
exactly `/api/auth/job` and the `JobRequest` schema, changed exactly the
`CreateUserRequest` schema by one optional property with a safe default and no
change to its required list, and redefined no path item. The other diffs in
that measurement were 12 paths temporarily absent during W0-1's split and one
path plus two schemas from W0-3.

**One declared helper**, under the 450-line rule and the
`<parent stem>_<role>.py` naming rule: `kairos_api/auth_store_password.py`.
`auth_store.py` was 420 lines before the job field and would have been 470
after it, so the scrypt block moved out verbatim and `auth_store.py` re-exports
it. Every caller still reaches `store.verify_password`,
`store.burn_password_check` and the `SCRYPT_*` constants unchanged, the record
shape and the parameters are identical, and every password already on disk
still verifies, which `tests/test_auth.py` proves end to end through a real
login. `auth_store.py` is now 433 lines.

## Rule 1: the affiliation wall and the `can_edit` contract

`kairos_api/affiliation_wall.py`. One `Wall` declares a surface's permission
rule once and serves it three ways.

```python
from kairos_api.affiliation_wall import Wall

MODEL_WALL = Wall(detail="התצוגה הזו שמורה לצוות החברה", company_only=True)


@router.get("/api/model/gates")
@MODEL_WALL.guard()                     # a viewer reads it, a channel account does not
def gates() -> dict:
    return MODEL_WALL.stamp(build_gates(), request)


@router.post("/api/model/gates")
@MODEL_WALL.guard()                     # and only a write role gets past this one
def set_gates(payload: GateRequest) -> dict:
    ...
```

That is the whole adoption: one declaration and the same decorator on every
route of the surface. The decorator applies the gate each method is subject to,
so a piece does not decide per route and cannot get it wrong per route.

| Use | Call | Effect |
|---|---|---|
| Gate a whole route | `@MODEL_WALL.guard()` under the route decorator | 403 before the handler runs: affiliation on every method, role on the write |
| Gate a route where seeing is itself privileged | `@MODEL_WALL.guard(roles_on_read=True)` | the strict form, role on the read too |
| Gate a write branch | `MODEL_WALL.require(request)` | raises `HTTPException(403, detail)` |
| Gate a read branch | `MODEL_WALL.require_read(request)` | the same, affiliation only |
| Tell the truth before the click | `MODEL_WALL.stamp(body, request)` | writes `can_edit`, and `can_edit_reason` only when false |
| Ask about changing | `MODEL_WALL.allows(request)`, `MODEL_WALL.reason(request)` | bool, and the detail or None |
| Ask about seeing | `MODEL_WALL.allows_read(request)`, `MODEL_WALL.read_reason(request)` | the same pair, affiliation only |

`Wall(detail, company_only=True, roles=WRITE_ROLES, role_detail=...)`.
**Affiliation decides seeing and role decides changing, so affiliation closes
every method and role closes only the write.** The reason names the gate that
actually closed: a channel administrator is refused the read on affiliation, a
company viewer is refused the write on role and reads everything its side of the
line holds. `roles=frozenset()` drops the role gate entirely, which is a wall
that anyone on the right side of the line may write.

**This is the second round of that sentence, and the first round was wrong in a
way that would have reached eighteen pieces.** `guard()` originally enforced both
gates on every method, so the declaration above, adopted exactly as this contract
demonstrated it, answered **403 to a company viewer asking for a read**, with the
read-only-role refusal. Measured on real resolved sessions before the fix, with
this file's own one-liner: viewer `GET` 403 `לחשבון צפייה אין הרשאת עריכה`,
viewer `POST` 403, channel `GET` and `POST` 403, admin 200 and 200. And
`company_only(detail)` shipped with `roles=frozenset()`, so the mirror hole was
open at the same time: a company **viewer could POST** through it, 200. Both
directions are closed now and both are pinned by
`tests/test_w0_cleanup_wall_reads.py`, ten tests through a live client on real
sessions. The two walls this piece actually shipped are the evidence that the
gates were wrong rather than the walls: `ACTIVATION_WALL` documents "a company
viewer sees the state and not the control" and `GUARDRAIL_WALL` documents "a
channel account reads them and an admin changes them", and neither sentence was
true of its own route under the first round.

`session_for(request)` returns `{username, role, affiliation}` or None.
`is_company(request)` and `require_company(request, detail)` delegate to
`events_access`, so there is exactly one implementation of the affiliation
question in the process and the five shipped `require_company_editor` call
sites are untouched.

**Unknown identity is permitted**, exactly as the events guard already behaves:
auth disabled, no request object, or no resolvable session all read as company
and as permitted. The server middleware answers 401 before any route runs, so
this is not a hole, and it is what keeps a deployment without login open.

**The decorator keeps FastAPI's injection working.** A route that declares
`request: Request` is called unchanged. A route that declares none gets one
appended to the signature FastAPI reads, under the reserved name
`kairos_wall_request`, and stripped before the handler is called.

**The wall finds the request by type, never by name, and an adopter needs to
know why.** A parameter called `request` is not necessarily a `Request`:
measured on the live app, **8 of the 115 routes call a request body model
`request`, and all 8 are writes**, `POST /api/assistant/ask`,
`/api/assistant/ask/stream`, `/api/scenario`, `/api/optimizer-plan`,
`/api/optimal-plan`, `/api/scenario-compare`, `/api/jobs/recompute` and
`/api/break-decisions`. Every one of them belongs to a piece this spec tells to
adopt the wall: P9, P2 and P1. A wall that trusted the name took the
declared-`Request` branch on those routes, found a Pydantic model where it
expected a `Request`, read the identity as unknown, and unknown identity is the
tolerant path, so the route answered **200 to the same channel account the
identical route answered 403 to**. The reserved injected name is the other half
of the same fix: `Signature.replace` raises `ValueError: duplicate parameter
name: 'request'` if the injected parameter keeps that name, so the injected
branch could not have rescued those 8 routes either.

**The direction of doubt is closed, not open.** When the type cannot be
resolved the answer is "no declared Request", an extra one is injected under the
reserved name, and FastAPI fills the route's own parameter as it always did. An
adopter therefore cannot open a hole by naming a parameter anything at all.

**And it keeps every other parameter working, which is the guarantee an adopter
actually depends on.** `@guard()` is safe on a route with a request body, a path
parameter, a query parameter, a dependency or a model return annotation, and the
walled route publishes the same OpenAPI operation as the identical unwalled one.
That is not free, and the first round of this piece did not have it. The wrapper
is defined inside `affiliation_wall.py`, so `wrapper.__globals__` is that module,
and FastAPI resolves a published signature's string annotations against the
callable's globals. Measured: 79 of the 80 modules in `kairos_api` declare
`from __future__ import annotations`, so every route annotation reaches the
wrapper as a string, and a body model declared in the route's own module does not
exist in `affiliation_wall`'s namespace. The parameter fell through to a query
parameter and the route answered
`422 {"loc": ["query", "payload"], "msg": "Field required"}` to the body it was
sent, and `GET /openapi.json` raised
`PydanticUserError: TypeAdapter[...] is not fully defined`, which is the spec's
own bar in section 8.2 for every later piece's `server.py` append.

The fix is one call, `_resolved_hints`, which evaluates the annotations with
`typing.get_type_hints(func, include_extras=True)` against the route module's own
globals before the signature is published, in the declared-`Request` branch and
the injected branch alike. `include_extras` keeps `Annotated[..., Depends(...)]`
intact. Only a string annotation is replaced, so a module that does not postpone
its annotations gets back the signature it already had.

**The measurement, over the real package rather than a fixture.** Wrapping every
route in `kairos_api` and comparing FastAPI's own `get_dependant` classification
before and against after: **48 of 133 route and path pairs were misclassified by
the first-round wrapper, 44 of them a request body silently becoming a query
parameter and 3 failing dependant construction outright; 0 of 115 are
misclassified now, with 0 errors.**

That sweep is necessary and not sufficient, and the difference is worth stating
because it is what hid the name bug for a round. The name-trusting wall injected
nothing on those 8 routes, so the parameter shape was unchanged and the
classification sweep was clean while the wall was blind. **The sweep that
discriminates asks whether the wall receives a real `Request` on every route:
8 of 115 blind before, 0 of 115 blind after.**

`tests/test_w0_4_affiliation_wall.py` carries the regression in three tests, one
through a live client, one on the published signature itself, and
`test_a_body_model_named_request_does_not_open_the_wall`, which refuses only a
**resolved** session so that unknown identity stays tolerant. That last detail is
the whole test: a blanket refusal also refuses unknown identity, so it cannot
tell a wall that resolved a session from one that failed to, and it passed
against the broken wall. All three fail against the logic they replaced.

**The four open doors this closes**, when their owners adopt it:
`GET /api/impact` and `GET /api/model/audience` are P7's, `GET /api/parameters`
is W0-4's remaining work, and `/api/events`'s `model_context` is P5's.

## Rule 2: the competitor boundary

`kairos_api/channel_scope.py`. The operator owns exactly one channel, read from
`settings.operator_channel`.

```python
from kairos_api import channel_scope

rows, scope = channel_scope.scope_records(rows)          # payload records
frame, scope = channel_scope.scope_frame(frame)          # a pandas frame
```

Both return `(data, note)`. The note is the disclosure that travels with the
payload: `scope_channel`, `scoped`, `rows_in`, `rows_out`, `channels_in`,
`competitor_rows_excluded`, `competitor_channels_excluded`, `reason`. With no
configured channel the data passes through unchanged and `reason` says why, so
a surface that cannot scope says so rather than quietly serving the market
total as the operator's.

`competitor_aggregate(records, sum_fields=(...))` is the only shape in which a
competitor may reach a payload: `{channels, rows, totals}`, no name, no
per-channel breakdown, no ranking. It exists because the retention model
genuinely needs the lineup as a factor.

**The measured breach, on the reference data with `operator_channel = רשת 13`,
taken from the live instance before this module existed.** `GET /api/schedule`
serves `break_schedule` as `head(200)` of the plan: 96 `קשת 12`, 73 `כאן 11`,
28 `עכשיו 14`, and 3 of the operator's own. Its `rows` canvas carries 1,852
programmes across four channels, of which 1,328 are competitors'. Its nested
`break_operations` carries 12 programmes per channel. `GET /api/break-operations`
carries 12 programmes for each of the four channels, 48 in total.

**Who applies it.** P2 on `/api/schedule`, P3 on `/api/break-operations`, P9 on
Kai's context. C1 verifies. This piece does not edit those routes.

## Rule 3: the vocabulary

`tv-break-dashboard/src/vocabulary.js`, dependency-free so any tree can import
it.

```js
import { word, genreLabel, pricingClassLabel } from '../vocabulary.js';

word('object.break', locale);        // ברייק / break
word('action.run_plan', locale);     // הריצו את התוכנית / Run the plan
```

72 concepts, each with exactly one English and one Hebrew word, namespaced
`activity.`, `action.`, `place.`, `object.`, `concept.`, `state.`, `role.`,
`affiliation.`. An unknown key returns an empty string, so a missing word is a
gap a reviewer notices rather than a token a person has to decode.

**The retired words are absent from the file**, not commented out and not kept
as aliases. The critic's grep for recompute, rebuild, חישוב מחדש and
בנייה מחדש under `tv-break-dashboard/src/` returned 159 hits before this wave;
zero of them are in this file, and a test in
`tests/test_w0_4_vocabulary.py` fails if one appears.

**The two value sets that used to share a name.** Measured:
`output/weekly_break_schedule.csv` carries 15 distinct `program_type` values,
while `config/optimization_weights.yaml` and `models/tv_break_coefficients.json`
price and measure on 4. They share only `News` and `Other`. So there are two
concepts, not one with four names:

- `concept.programme_genre`, values in `PROGRAMME_GENRES`, 15 members, read
  with `genreLabel(value, locale)`. This is what a scheduler reads.
- `concept.pricing_class`, values in `PRICING_CLASSES`, 4 members, read with
  `pricingClassLabel(value, locale)`. This is what the rate card and the
  retention model use.

A test compares both lists against those files, so a value set cannot drift
from the file that produces it.

## Rule 4: the session

`tv-break-dashboard/src/session.js`, dependency-free for the same reason.

```js
import { fetchSession, payloadCanEdit, doorFor, WALLS } from '../session.js';

const { session } = await fetchSession();
const { canEdit, reason } = payloadCanEdit(payload, session, WALLS.events);
```

`normalizeSession(body)` turns `GET /api/auth/me` into
`{authDisabled, username, displayName, role, affiliation, job,
mustChangePassword, isCompany, isAdmin, canWrite}`. Login not set up is an
honest state of its own, not a fake identity, and every gate reads open, which
is what the server does.

**The endpoint is the authority.** `payloadCanEdit(payload, session, wall)`
uses the payload's own `can_edit` and its reason verbatim whenever the payload
carries one, and falls back to `sessionCanEdit(session, wall)` only for an
endpoint that has not been given one yet. `WALLS` holds the six refusal
strings, and a test pins each against the Python constant that would be sent,
so the words a person reads before the click and the words the server sends
cannot drift.

**The job field.** `job` is on the account record with `unset` as the default,
so every record that predates it reads unset and nobody's access or screen
changed. Thirteen values, `auth_store.JOBS`, in the same order as
`session.js`'s `JOBS`, pinned by a test:

| Job | Door | Job | Door |
|---|---|---|---|
| `general_manager` | `today` | `campaign_manager` | `clients.campaigns` |
| `planner` | `plan.week` | `analyst` | `clients.money` |
| `scheduler` | `plan.day` | `data_steward` | `sources.today` |
| `traffic_operator` | `plan.tonight` | `account_administrator` | `account.accounts` |
| `programming_representative` | `rules.restrictions` | `model_steward` | `model.console` |
| `compliance_owner` | `rules.licence` | | |
| `yield_owner` | `rules.rate_card` | | |
| `account_manager` | `clients.all` | | |

Thirteen doors, no two roles sharing a first screen. `DOORS` is complete and
freezes with the file: a later piece implements the route for its own door id
and adds nothing to the map. `doorFor(session)` returns `today` for an unset
job and for a channel account whose job is the company-only one, so nothing
tells a channel account the other side exists. `jobPickerRows(session, locale)`
is the picker card's contract: 13 rows for a company account, 12 for a channel
account, each `{id, door, label, doorLabel}`. `needsJobPicker(session)` is
false when login is not set up, because there would be no record to write to.

**`PUT /api/auth/job`**, body `{"job": "planner"}`, returns the account record.
Self-service by design: a job decides a landing screen, not a permission, so it
sits outside `/api/auth/users*` and a viewer may set their own. An unknown
value answers 400 and changes nothing. An administrator may also set `job` in
the `POST /api/auth/users` body; it is optional and defaults to `unset`.

The account payload gained exactly one key. It is now `username`,
`display_name`, `role`, `created_at`, `must_change_password`, `affiliation`,
`job`.

## Rule 5: the guardrails and the activation switch

`kairos_api/guardrail_store.py` with `data/regulatory_guardrails.json`.

```python
from kairos_api import guardrail_store

body = guardrail_store.payload(request)                  # the read, with can_edit
guardrail_store.require_guardrail_editor(request)        # the distinct permission
guardrail_store.record_change({"max_breaks_per_hour": 3}, effective="2026-09-01",
                              actor=username, reason="regulator circular")
```

The store holds the four numbers that are the licence:
`max_ad_minutes_per_hour` 12.0, `max_breaks_per_hour` 4,
`min_break_spacing_minutes` 7, `protected_program_max_ad_minutes_per_hour` 8.0,
with `effective_date` 2026-06-14 and `source_url` https://www.rashut2.org.il/.
Every one of those is the value `KairosSettings` ships, and a test fails if the
two drift while both exist.

- **The effective date.** `values_on(day)` answers what the limits were or will
  be on any day. `effective_date(day)` names the day the limits in force took
  effect. A change recorded today for next month does not move a number today.
- **The change record.** `record_change` appends `{effective_date, recorded_at,
  actor, reason, values, before}` and writes atomically. The log is append-only
  and `changes()` returns it oldest first. `changed_since(day)` is the
  attestation answer: an empty list is the evidence that no guardrail moved.
  `scheduled_changes(day)` is the alert: a limit about to move is visible
  before the day it moves.
- **The distinct permission.** `GUARDRAIL_WALL` is role-gated on `admin` and
  not gated on affiliation, because the limits are the operator's own licence:
  a channel account reads them, an admin changes them, and the operator who can
  move the revenue slider cannot move the licence. The refusal is
  `עריכת מגבלות הרגולציה שמורה למנהל המערכת`.
- **Values are validated** against the same bounds `KairosSettings` declares,
  so a value refused there is refused here. A refused change leaves no trace.

**The cutover, for P5, is one line.** `settings_overlay(settings)` returns the
settings with the four values in force applied and every other field untouched.
It is an exact identity today, proven on both the shipped defaults and the
deployed `data/kairos_settings.json`, so adopting it moves no number. Until it
is adopted, `KairosSettings` keeps the four fields and `GET /api/settings`
answers exactly what it answered before this piece.

`kairos_api/model_activation.py` is the audience model activation switch.

```python
from kairos_api import model_activation

body = model_activation.payload(request)             # the read, with can_edit
model_activation.set_active(True, request)           # the gated write
```

`payload` returns `{field, active, state, computed_at, consequence_he,
consequence_en, can_edit}` plus `can_edit_reason` when refused. `state` is the
engine's own three-state basis: `off`, `on`, `on_no_artifact`. It carries no
gate verdict, no coverage, no coefficient and no p-value, and a test greps the
payload against the section 4.2 lexicon, so this stays true.

`ACTIVATION_WALL` is company-only with the Hebrew denial
`הפעלת מודל הקהל שמורה לצוות החברה`, built exactly like the event pricing
denial beside it. `set_active` saves through the same settings seam every other
configuration act uses, so the freshness fingerprint sees the flip and the
saved plan is marked out of date exactly as it is today.

Both stores hand over to **P5**, not to P7. The switch is a run-side
configuration act and its surface is Rules. P7 renders a read-only mirror and
owns no control.

## Bar 3 evidence

| Claim | Evidence |
|---|---|
| The wall fix cannot have moved a shipped number | Zero shipped routes call `guard()` or `company_only()`, by grep over `kairos_api/`. The symbols the fix changed are read only by `_wrap_with_wall`, which only `Wall.guard()` reaches, so `require`, `stamp`, `reason` and `is_company` are untouched paths |
| The read-gate correction is the same kind of no-op on the shipped surface | Still zero call sites, by the same grep: the only two shipped walls, `GUARDRAIL_WALL` and `ACTIVATION_WALL`, use `stamp` and never `guard`. `reason` and `stamp` keep answering the write question unchanged, so `can_edit` on every payload is the value it was. `tests/test_w0_4_affiliation_wall.py`, `test_qa8_permissions.py`, `test_auth.py`, `test_w0_4_guardrails.py` and `test_w0_4_model_activation.py`: 45 passed, unmodified |
| The app's published contract is byte-identical under the old and the new wall logic | `app.openapi()` sha256 `4f44e6f4...b953e60b` and the 115-route set sha256 `2d7a7cbe...e5e285f2`, equal in both processes |
| The three event writes still refuse a channel account with the existing Hebrew denial | `tests/test_qa8_permissions.py` 9 passed, unmodified, including `test_channel_user_reads_events_but_every_write_is_403` |
| `GET /api/events` still returns `can_edit` | same file, `can_edit` true for company and admin, false for channel |
| The five `require_company_editor` call sites still fire | unchanged: `events_api.py:378,399,426` and `pricing_api.py:234,241`. `events_access.py` was not edited |
| The account payload changed by exactly one additive key | `tests/test_w0_4_session_job.py::test_the_account_payload_grew_by_exactly_one_key` |
| No existing endpoint changed its body | `/api/settings`, `/api/events` and `/api/pricing` byte-identical before and after. `/api/compliance` could not be compared: it answered 404 mid-measurement because W0-1's split had lifted it out of `dashboard_api.py` and had not remounted it yet |
| The OpenAPI surface grew by exactly the one published path | one path added, one schema added, one schema gained one optional property, no path item redefined |
| The guardrail cutover moves no number | `settings_overlay` is an exact identity on the deployed settings and on the defaults |
| Adjacent suites green | `test_auth.py`, `test_qa8_permissions.py`, `test_qa2_sec_hardening.py`, `test_activity_log.py`, `test_version_store.py`, `test_api_surface_qa.py`, `test_uploads_versions_security.py` |
| The frontend still builds | `npm run build`, 3,315 modules transformed |

New tests, all under the reserved prefix: `tests/test_w0_4_affiliation_wall.py`,
`tests/test_w0_4_channel_scope.py`, `tests/test_w0_4_guardrails.py`,
`tests/test_w0_4_model_activation.py`, `tests/test_w0_4_session_job.py`,
`tests/test_w0_4_vocabulary.py`. The read-gate correction, made after this piece
closed, adds `tests/test_w0_cleanup_wall_reads.py`.

## What this piece deliberately did not do

- **It did not scope a route.** `/api/schedule` and `/api/break-operations` are
  P2's and P3's. The helper and the measurement are here; the edit is theirs.
- **It did not close `GET /api/parameters`.** It is on nobody's row in section
  8.2 and it is served from a module this piece does not own. It needs a ruling
  before it can be walled.
- **It did not remove the four guardrail fields from `KairosSettings`.**
  Removing them changes `GET /api/settings`, which is a visible change in a
  refactor wave. The store is authoritative from the moment P5 calls
  `settings_overlay`, and the drift test keeps the two honest until then.
- **It did not add the `core.py` compatibility shim.** The shim exists to keep
  readers importing unchanged when the fields leave `KairosSettings`. Nothing
  has left, so nothing needs shimming, and `core.py` is already 698 lines,
  which is over the file-size cap.
