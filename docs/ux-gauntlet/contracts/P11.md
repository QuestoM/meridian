# P11 Pacing and make-good: the published contract

The section 8.8 publication for P11: the endpoints it owns, the payload shapes it
emits, the files from the section 8.2 wave-two row it writes, and the helper
modules it declares under the section 8.2 naming rule.

Written in round one, revised in round two when the second ending, the day drill
and the four stylesheets landed.

---

## 1. The files this piece writes

**From the section 8.2 wave-two row.** `kairos_api/pacing_alerts_api.py`,
`kairos_api/makegood_store.py`, `data/make_goods.csv`, and
`tv-break-dashboard/src/clients/pacing/**`.

**Helper modules, named `<parent stem>_<role>.py` beside their parent.**

| Module | Parent | What it holds |
|---|---|---|
| `kairos_api/pacing_alerts_api_board.py` | `pacing_alerts_api.py` | The pacing arithmetic and the four ways it refuses to state a pace |
| `kairos_api/pacing_alerts_api_read.py` | `pacing_alerts_api.py` | The reads the board stands on, the competitor boundary, and the deficit ladder |
| `kairos_api/pacing_alerts_api_words.py` | `pacing_alerts_api.py` | Every string and the two published triggers |
| `kairos_api/pacing_alerts_api_write.py` | `pacing_alerts_api.py` | The row an act composes, and the words a refused transition uses |
| `kairos_api/pacing_alerts_api_wire.py` | `pacing_alerts_api.py` | What the board sends over the wire, once instead of per row |
| `kairos_api/makegood_store_words.py` | `makegood_store.py` | The controlled values, the vocabularies and the published sentences |

The last two were added after the first publication of this contract, when
`makegood_store.py` passed the size law and when the board payload was measured
at 184,610 bytes for 56 rows. Both sit under the same naming rule and neither
collides with a path another piece owns.

**Four stylesheets, one per component, all imported by `PacingWorkspace.jsx`.**
`pacing.css` is the workspace and the board chrome, `pacing-row.css` one row,
`pacing-days.css` the day drill, `makegood.css` the ledger. They are split on the
component boundary rather than by size alone, and no piece may define a token so
every one of them reads `tokens.css`.

**No verification script beside the surface.** The bundler driver that proves
this tree compiles lives inside `tests/test_p11_surface_javascript.py` as
`PARSE_DRIVER`, and is executed with `node --input-type=module --eval` from the
frontend package so the bare `rolldown` specifier resolves. It was a separate
`.mjs` file for one round and that round measured why it must not be: the script
was never committed while the test that shelled out to it was, so at HEAD the
one guard that can see a syntax error here could not run. See section 6.

**Components, all under `src/clients/pacing/**`.** `PacingWorkspace.jsx` is the
panel and the only thing the mount touches. `PacingBoard.jsx` is the list,
`PacingRow.jsx` one row, `PacingActs.jsx` the two controls a row offers,
`PacingGoalLine.jsx` the campaign's other goal, `PacingDays.jsx` the day drill
and `MakeGoodLedger.jsx` the ledger. `pacing-api.js` is every read and write,
`pacing-helpers.js` the readings, `pacing-summary.js` the two counting sentences
above the list, and `pacing-place.js` the reader's place across leaving the
board. The last four hold no JSX on purpose: node executes them directly, so the
arithmetic behind a sentence is asserted rather than greped for.

**Test files**, under the reserved `tests/test_p11_*` prefix: `test_p11_pacing_board.py`,
`test_p11_make_good_ledger.py`, `test_p11_regression.py`, `test_p11_surface_remedy.py`,
`test_p11_design_rules.py`, `test_p11_surface_regression.py`, `test_p11_surface_javascript.py`,
`test_p11_surface_reach.py`.

`test_p11_surface_regression.py` and `test_p11_surface_javascript.py` are splits
of `test_p11_regression.py`, which reached 664 lines under a size guard that read
the modules and the surface and never the tests. The division is by what a test
can see: the regression file reads the API, the stores and the words; the surface
file reads the components as text; the javascript file executes the shipped
module in node. That third boundary is the one that matters, because a guard that
greps for a string cannot see a defect about syntax at all.

`test_p11_surface_reach.py` is the round-three file and it is split by subject
rather than by size: every assertion in it is about a thing the payload already
held and the screen gave a reader no way to reach.

**No `server.py` append.** W0-1 created and mounted `pacing_alerts_api.py` in wave
zero, so the new routes ride the router that is already registered.

---

## 2. The endpoints

| Method and path | What it answers |
|---|---|
| `GET /api/pacing` | Every campaign on the operator's channel, worst pacing first, with both goal lines and either a verdict or the named reason there is not one. It carries a day count per row and not the days themselves |
| `GET /api/pacing/{campaign_id}/days` | The broadcast days behind one row, read when a reader opens them. Measured, the days were 181,274 of a 365,884 byte board; on their own read the board is 184,610 and a drill is 2,838 |
| `POST /api/pacing/{campaign_id}/accept` | Record that the risk on this campaign stands. Body is `{note}` and carries no figure. Refused on a row the board is not asking a decision about |
| `GET /api/make-goods` | The ledger, newest first, as one timeline in `decisions` plus the two kind-specific lists, with the sign-off rule published as not configured |
| `POST /api/make-goods` | Raise one against a campaign's measured shortfall. Body is `{campaign_id, note}` and carries no figure |
| `POST /api/make-goods/{make_good_id}/state` | Move one. Body is `{state, offer_value, offer_window_start, offer_window_end, reason, note}` |

**Both writes answer with the act that reverses them.** `POST /api/make-goods`
and `POST /api/pacing/{id}/accept` return `{make_good, undo}`, where `undo` is
`{state, reason, label_en, label_he, meaning_en, meaning_he}`. The same block is
published on the ledger read at `vocabulary.undo`.

An undo is not a fourth act and it deletes nothing. It is this ledger's own
withdrawal, carrying the one published reason that says the record should not
have been opened, taken from `close_reasons[withdrawn]` rather than from a
constant a client wrote. The record stays on the ledger, closed, with who
withdrew it and when. It is published because both writes can be fired by a
single keystroke on a focused row, and the banner that confirms one has to be
able to offer the way back without holding its own copy of the rule.

`GET /api/make-good-alerts` is unchanged, is not wrapped by the wall, and still
answers `data_available: false` with the `campaign_flights.csv` reason. It is the
signal `kairos/optimize/pacing.py` feeds the optimizer with, and two shared tests
assert its exact answer.

All five new routes carry `PACING_WALL = Wall(company_only=False)`: any account
reads, a write role changes, and `can_edit` is stamped on every read.

**`can_edit_reason` is joined by a pair.** The wall stamps one string and the
constants behind it are Hebrew only, so an English reader on a viewer account met
a Hebrew sentence with every other word on the screen in English. Every payload
this piece stamps carries `can_edit_reason_en` and `can_edit_reason_he` beside the
wall's own string, and only when this piece holds a translation for it. The
Hebrew is `affiliation_wall.READ_ONLY_ROLE_DETAIL` itself rather than a copy, so
a Hebrew reader's sentence is unchanged and the two cannot drift. The wall is
frozen and is read, never written.

---

## 2a. The two endings, because the job has two

JS-6 is done when every at-risk campaign has an act taken against it **or an
explicit decision to accept the risk, and both are recorded**. A ledger of raises
alone leaves a campaign somebody read and accepted looking exactly like one
nobody opened, so the store holds two kinds.

- A **make_good** is raised against a measured shortfall and is offered, settled,
  declined or withdrawn.
- An **acceptance** is the recorded decision that the risk stands. It stamps the
  same measured figures a raise would have taken, derives nothing, never becomes
  an offer, and moves only to withdrawn.

The duplicate check is **per kind**. A shared one would have silently forbidden
raising a make-good against a campaign whose risk somebody took on last week,
which is the ordinary case.

Both kinds ride one ledger read, because a person answering for a campaign has to
see both. `decisions` is the joined timeline and is what a surface renders;
`make_goods` and `acceptances` are the kind-specific lists beside it.

---

## 3. The honest math, stated so a critic can check it rather than trust it

Two stores are read and neither is written: P4's campaign store, and P4's derived
delivery ledger, which states per campaign per broadcast day whether the traffic
log holds those spots (`aired`, `scheduled`) or holds nothing for that day
(`unknown`).

**The clock is the ledger's, never the reader's.** `counted_as_of` is the instant
the ledger split aired from scheduled at. Nothing in this piece calls the wall
clock, so every figure is reproducible.

**Pace to date is stated only when the elapsed window is complete.** The flight's
broadcast days on or before the counted day are the elapsed window. When every one
of them carries a source, delivered to date is a total and a ratio against it is
real. When one is missing, the verdict is `unknown` with code `gap_in_elapsed` and
the missing dates are listed, because a ratio computed over a hole would tell a
reader a campaign is behind when what is true is that nobody counted a day of it.

**The reference is an even share and says so.** `expected_through_counted_day` is
`goal * days_counted / days_in_flight`, published with its rule in both languages.

**The two triggers are policy and are published.** 0.95 and 0.85, on the payload,
with the sentence that they are the product's stated rule and not a commercial
term, and that the commercial trigger is owner-pending.

**The forward half fails separately.** `covered`, `short_certain` (every remaining
day sourced and still short, so the shortfall is measured rather than projected)
and `not_booked_yet` (remaining days carry no source, so the rest cannot be
projected). Nothing is extrapolated in any of the three.

**The deficit ladder, most certain first.** `measured_closed` for a flight that has
ended with every day sourced, `booked_short` for a running flight whose remaining
days are all sourced and short, `to_date` for the gap against the reference at the
counted day. A campaign that reaches none of them cannot carry a make-good and the
raise is refused with the reason.

**The surface offers a make-good only when the shortfall is owed.** A campaign
behind on day one of seven with unbooked days ahead offers the booking, not a
debt. `tests/test_p11_surface_remedy.py` holds the same behind-pace campaign in
both shapes and asserts the offered act changes.

---

## 4. What this piece deliberately does not do

- **It does not propose compensating inventory.** The spec's make-good wants the
  offer drawn from the break board. An offer here is the value and the window a
  person entered, and the payload says in both languages that it reserves nothing.
  Drawing an offer from real inventory needs the break's spot-level contents,
  which is P10.
- **It does not decide who signs one off.** The commercial rule was never supplied,
  so the ledger records who acted and publishes `sign_off.configured: false` with
  the path to supply the rule.
- **It does not touch `campaign_flights.csv`.** Owner decision 4's three inputs are
  still outstanding. What became buildable is the delivery ledger P4 derived from
  the traffic file on disk, which is real for the days it covers and honestly
  unknown for the days it does not.

---

## 5. Open, for the lead: the mount

`tv-break-dashboard/src/clients/pacing/**` is P11's under section 8.2 and it sits
inside `tv-break-dashboard/src/clients/**`, which is P4's. The Clients destination
renders its views from two P4 files, so the surface this piece ships is not
reachable from the running shell without four lines in files P11 may not write.
This is the same shape as P13 under P10 and section 8.2 does not name a handover
for it, so it is raised rather than reached for.

**The exact patch. Three sites, not two.** The round-one version named two and the
address did not survive a reload, because `VIEWS` gates the `?clients=` query
parameter. Measured before the correction: a reload of `?clients=pacing` landed on
`?clients=campaigns`. All three were applied to a scratch copy of the tree in
round two, built with vite, served and driven in a browser, and the whole of the
evidence below was measured through them.

In `tv-break-dashboard/src/clients/ClientsChrome.jsx`, one row at the end of
`VIEW_LABELS`. The Hebrew is the controlled word from `src/vocabulary.js`
`concept.pacing`, not the `קצב` the round-one patch wrote:

```js
  { key: 'pacing', en: 'Pacing', he: 'קצב אספקה' },
```

In `tv-break-dashboard/src/clients/clients-money-helpers.js:11`, one entry, without
which the tab writes an address that does not restore:

```js
export const VIEWS = ['clients', 'money', 'campaigns', 'advertisers', 'agencies', 'pacing'];
```

In `tv-break-dashboard/src/clients/ClientsWorkspace.jsx`, one import and one
branch beside the five that are already there:

```js
import PacingWorkspace from './pacing/PacingWorkspace';
...
          {active === 'pacing' ? (
            <PacingWorkspace locale={locale} notify={notify} refreshKey={refreshKey}
                             onOpenCampaign={openCampaignRecord} />
          ) : null}
```

`onOpenCampaign` is the fourth prop and it is not optional decoration. Without it
the campaign name on a board row and on a ledger record has nowhere to go, and
the five rows whose path forward reads "Open the campaign and set a goal on its
flight" offer no control that does it. `ClientsWorkspace` already holds
`openCampaignRecord(campaignId)`, which is exactly the seam the prop expects. The
panel treats the name as a control only when the prop is supplied, so a mount
without it regresses nothing and simply does less.

**And `notify` has to reach this file to be worth passing.** Measured twice, a
round apart: `src/shell/workspace-router.jsx:152` renders `CampaignsPage` with no
`notify` prop, and `CampaignsPage.jsx` contains the word zero times, so
`ClientsWorkspace` falls back to its no-op default and every notice this panel
sends is swallowed. Driven in a browser, a successful acceptance produced no
toast at all. The panel now prints its own notice as well as sending one, so a
write that lands says so whatever the shell does, and the toast is a second copy
rather than the only one. Both files are outside this piece.

`PacingWorkspace` takes `locale`, `notify`, `refreshKey` and an optional
`onOpenCampaign(campaignId)`, and needs nothing else from the workspace it is
mounted in. It renders an `h2` and not an `h1`, and puts its two view controls on
the same row as that heading, because the destination it mounts into already
carries an `h1` and a view strip: measured, the stacked pair cost 90 px of the
fold and put a second `h1` in the document outline.

**Two rows for whoever holds P8.** Every pacing write does reach the product's
persistent activity feed and none of them is named there.
`history_api_actions.action_for` returns `other` for all three pacing paths, so
History shows a raw API path under a generic word while settings and preview get
specific ones. Measured on a running server: an acceptance recorded as
`{user admin, method POST, path /api/pacing/CMP_D030/accept, status 201, outcome
applied, action other}`. Two rows in P8's own table close it, and
`tests/test_p11_regression.py` pins the current answer so the change is visible
when it lands.

**A second thing for whoever holds P4.** `MakeGoodAlerts.jsx` still tells the
reader that under-delivery alerts need `campaign_flights.csv` uploaded. That was
true when it was written and it is now only half true: the older projection does
need that file, and the pacing board above does not. The panel's own sentence and
the retired word `rebuild` in `campaigns_delivery.py`'s path-forward string are
both P4's to change.

---

## 6. The surface did not compile, and nothing in the run could have told you

Measured this round, and the most important thing on this card.

`PacingWorkspace.jsx` opened a `<Figure>` and closed it with `</span>`. The whole
of `src/clients/pacing/**` failed to compile, so applying the three-line mount in
section 5 would have broken the Clients destination on the first attempt, and the
mount would have been blamed for it.

Nothing could see it. `npm run build` transforms 3,510 modules and not one of them
is in this tree, because nothing imports it, so the repo build passed clean with a
syntax error sitting in a shipped file. Every guard on this surface read a
component as text, and text guards pass over a mismatched tag without noticing.
The tests were green, the build was green, and the artifact did not exist.

The fix is one word. The guard is the part worth keeping:
`tests/test_p11_surface_javascript.py::test_every_component_on_this_surface_actually_compiles`
runs the real bundler with everything outside this tree external and the
stylesheets stubbed. Proven to fail against the defect and pass against the fix.

**And the guard itself had the same disease.** It ran the bundler from a script
beside the surface, `verify-parses.mjs`, which was never committed while the test
that shelled out to it was. So at HEAD the surface did not compile, the guard for
it could not run, and the previous round's own record said both were fixed. They
were fixed on disk and not in the repository. The driver now lives in the test
file as `PARSE_DRIVER` and the script is gone. Measured when that landed: the
driver, run over a `git archive HEAD` of the tree, answers `PARSE_FAIL, Expected
corresponding JSX closing tag for 'Figure', PacingWorkspace.jsx:267`, and over
the working tree `PARSE_OK`. The one-word fix is still uncommitted, because a
builder may not commit either.

**And the guard beside it was restated in round three, because the sentence it
asserted could not survive its own subject.** It read that nothing under
`src/clients/pacing` may exist only in a working tree, which is true of a
committed tree and false of every round that adds a file to this surface: a
builder may not commit, so a new component is untracked on the day it is written
and the assertion fails for the one reason that is nobody's defect. It now
asserts the class the incident was actually about, in
`test_no_guard_on_this_surface_stands_on_a_file_the_repository_does_not_have`:
every path a `tests/test_p11_*` guard names under this surface exists on disk,
which is exactly how the defect showed at HEAD, and no file of this surface is an
executable script for a guard to shell out to. Which files are waiting for a
commit is a blocker in the state file, which is where a thing that needs a commit
belongs.

**The general rule, for the lead.** A tree that nothing imports is not covered by
the build, whatever the build says. Every piece in this run that ships a
destination subtree behind an unapplied mount is in the same position, and a
green build is not evidence about it. P13 under P10 is the other one.

Measured after the fix, on a scratch copy carrying the section 5 mount: the tree
compiles, and the bundle grows from 1,623.16 kB to 1,659.43 kB of JavaScript and
from 305.75 kB to 319.41 kB of CSS, which is this surface arriving.

---

## 7. Both goals reach the row, because the job answers for both

Added in round three and the largest thing on this card after the mount.

A campaign in this store is booked against two goals: a rating goal in points and
a money goal in shekels. The payload has carried both on every row since the
first round, each with its own counted figure, its own even-share reference and
its own verdict. The row printed the one it leads with, and the other reached a
screen only as a bare pair under the day drill, with no verdict, no reference and
no ratio.

**Measured on the shipped board, which is why this is not decoration.** 48 of the
56 rows carry both goals. On 10 of those 48 the two verdicts disagree, and every
one of the 10 is a row the board is asking a decision about: it reads at risk on
rating and on pace on money. CMP_D040 is 0.88 against 0.9989. That is the
difference between a campaign spending to plan while under-delivering audience,
which is a conversation with the buyer, and a campaign behind on both, which is a
booking problem. Google Ads, the reference for this piece, is a spend-against-
budget board first.

One component states it, `PacingGoalLine.jsx`, rendered on the row under the
track and again under the day drill, so this product cannot come to hold two
statements of one goal. Four facts with a rule between them per design-rules.md
section 3: which goal, the counted figure against it, the ratio with its verdict
word, and the reference that ratio is against. A second line whose pace could not
be stated prints the reason rather than the bare word unknown.

Measured after: 48 goal lines on the board, all 10 divergences visible without
opening anything, and 48 occurrences of ILS on the English board where the round
before had zero.
