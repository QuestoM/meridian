# P3, Plan: the day and the break

The section 8.8 publication: the endpoints this piece owns, the payload shapes it
emits, the files from section 8.2 it writes, and the helper modules it declares.
Then the evidence for each of the four bars, measured rather than described.

The destination serves JS-3, the scheduler who moves one break, and the break
inspector. Its reference bar is a drawing tool for direct manipulation, selection
and undo, and a professional editing timeline for exact durations.

## 1. What this piece is

The product had no object below the programme segment. The saved plan carries a
break COUNT per segment, and the board a person looked at spread those counts
evenly across the programme as a display heuristic, so there was nothing to
click, nothing to address and nothing to attach money to. Section 3.4 of the
specification makes the break the money layer, and nothing in that section is
true until the break exists.

This piece builds it: a stable break identity, the day it lives on, the money the
plan credits to it, the acts a scheduler performs on it, and the exact inverse of
each act.

## 2. Endpoints owned

Nine operations over seven path items, all under `plan-day`. Measured on the live
instance: 7 path items, 9 operations, 0 paths redefined.

| Method and path | What it answers |
|---|---|
| `GET /api/plan/days` | the broadcast days the operator's own plan covers |
| `GET /api/plan/day` | one day as programmes and addressable breaks |
| `POST /api/plan/day/score` | what a rearrangement is worth, and whether it still complies |
| `POST /api/plan/day/save-effect` | what saving would really do, measured before anything is written |
| `GET /api/breaks/{break_id}` | one break, everything the plan knows about it |
| `POST /api/breaks/{break_id}/gold` | mark this break's programme gold |
| `DELETE /api/breaks/{break_id}/gold` | and take the mark off again |
| `POST /api/breaks/{break_id}/placement` | record a saved position and the restriction carrying it |
| `DELETE /api/breaks/{break_id}/placement` | and forget it, which is the undo half |

Plus the four routes W0-1 split out and handed over, unchanged in shape:
`GET /api/schedule/segments`, `GET /api/schedule/segment/{segment_id}`,
`GET /api/break-operations`, `GET /api/break-library`, `GET /api/gold-breaks`.

**Writing the restriction that carries a saved move is deliberately not a route
here.** The only store the weekly commit path reads for a placement is the scoped
constraint store, which is P5's and already has a shipped, tested write route.
The surface calls it and then records the break's own half of the transaction. A
second implementation of a constraint write would be a second set of rules.

## 3. Payload shapes

**Break identity.** `<segment_id>~<ordinal>`, for example
`2024-11-01|רשת 13|008~1`. The separator is a tilde rather than a hash because a
hash in a URL is a fragment delimiter: it never reaches a server unencoded, and
one caller that forgets to encode it turns a break id into a segment id silently.
A tilde is unreserved in RFC 3986.

**Money.** Every break carries `projected_revenue`, which is the optimizer's own
marginal revenue credited to that break at insertion, and `delivered`, which is a
tri-state and never an amount. The break's own record also carries the rating
that credit was computed at, `rating_at_this_break`, and the retention behind it,
because a basis that cannot reproduce its own figure is a wrong basis: see
section 8.
Measured on `רשת 13 / 2024-11-01` at the committed
settings: the 80 break credits sum to **1,067,845.57** against the day's own
**1,067,845.55**, a drift of 0.02 ILS from display rounding against a stated bound
of 0.005 per break.

**The footer prints the sum of the rows it is drawn under, and never the day's own
total.** An earlier round printed the day there, which was true only while nothing
was filtered. With the gold filter on it put 1,028,206 under a column of three
breaks worth 30,488, which is 33.7 times the figure it claimed to total, and with
no gold break in the day it left zero rows, no empty state and the same figure.
Both are closed. The day's own total is now a second line under its own label,
present only while a filter is on, and an emptied filter replaces the table and its
foot with a state that names the mark that is missing. Re-measured in a browser at
1440 x 823 after the fix, marking `001~1` gold from the day board so its three
breaks carry the mark: three rows at 10,712, 10,163 and 9,614, the footer 30,488
against the served credits 10,711.71 + 10,162.61 + 9,613.52 = 30,487.84, the day
line 1,028,206 against the served 1,028,205.58, and with the filter off 79 rows
totalling 1,028,206, which is the day. With no gold break the table and its foot are
gone and no figure is left standing under a row-sum label.

**That day total is the same number a different piece measured independently.**
`W0-5.md` section 5 reports `_optimize_one_day` on this channel-day at
1,067,845.5490407092 and its own scoring seam at 1,067,845.549040709. The board
serves 1,067,845.55. Three measurements, two pieces, one figure, which is the
strongest available evidence that this surface reports the plan's own money and
not a parallel calculation of it.

**Every figure carries its scope.** `basis` on both the board and the score
payload names channel, day, segment count, revenue weight, risk lambda, objective
mode, source and currency, and the surface prints channel and day beside each
figure rather than in a tooltip.

**Three honest states, never a figure.** No day to open (no channel or no plan),
delivered money (no spot ledger covers any planned day), and break contents (the
individual ads are not modelled until the owner's break identifier lands). Each
carries a machine `state`, a reason and a path forward, in both languages.

**A saved placement the plan no longer binds.** `GET /api/plan/day` also carries
`unbound_placements`: the day's saved records whose break id the plan no longer
has, each with `break_id`, `constraint_id`, the position it saved, a machine
`state` of `segment_replanned` or `segment_absent`, a tri-state `restriction` of
`in_force`, `absent` or `unknown`, and a reason and a path forward in both
languages. **The partition is exact and it is the point:** a saved record is
either reachable on its chip or served here, one of the two, never both and never
neither. Section 6 has the measurement that forced it.

**What saving would do, before the click.** `POST /api/plan/day/save-effect`
carries `before`, `after` and `delta` on the day's own totals, `rearranged`,
`compliance`, the `restrictions` it applied with their frozen-contract predicates,
`measured: true` and `engine_ms`. It builds the constraint rows a save would write,
runs the plan's own optimizer with them in force, writes nothing, and reports the
engine's own figures. Verified equal to the written save on both measured cases:
`-30,575.55` and `-47,444.20`, with the break count 80 and 78, gap 0.0.

## 4. Files written

Backend, all from the P3 row of section 8.2: `kairos_api/day_api.py`,
`kairos_api/gold_api.py`, `kairos_api/overrides.py`,
`kairos_api/break_api.py` (new), `kairos_api/break_store.py` (new),
`data/breaks.csv` (new), plus one stanza of two lines appended below the marker
in `kairos_api/server.py`. `kairos/export/spots.py` is on the row and was not
changed: it stands at 449 lines against the 450-line cap, so the coverage read it
needed became a declared helper beside it instead.

Frontend: `tv-break-dashboard/src/plan/day/**` and
`tv-break-dashboard/src/plan/break/**`.

**Helper modules declared under the section 8.2 naming rule**, each
`<parent stem>_<role>.py` beside its parent:

| Helper | Parent | Why it exists |
|---|---|---|
| `kairos_api/break_api_board.py` | `break_api.py` | shaping the board and scoring a rearrangement |
| `kairos_api/break_api_detail.py` | `break_api.py` | one break's full record |
| `kairos_api/break_api_states.py` | `break_api.py` | the three answers that are states rather than figures |
| `kairos_api/break_store_pins.py` | `break_store.py` | the register of placements an operator saved |
| `kairos/export/spots_coverage.py` | `kairos/export/spots.py` | which dates a spot ledger actually covers |

Frontend modules created inside the owned trees, which section 8.2 says need no
rule because the paths are globs: `DayPage`, `DayBoard`, `DayBoardChip`,
`DayBoardToolbar`, `DayBoardReadout`, `DayBoardSettlement`, `DayPicker`,
`day-board-model.js`, `day-board-drag.js`, `day-board-actions.js`,
`day-board-history.js`, `day-board-settlement.js`, `day-board-writes.js`,
`day-board-forecast.js`, `day-board-zoom.js`, `schedule-editor-pin.js`,
`OverrideDecisions`, `BreakBoard`, `BreakInspector`, plus `day-board.css`,
`day-chip.css`, `day-readout.css`, `break-board.css`, `break-inspector.css`.

`day-board-writes.js` holds the acts that change a store, and the exact inverse of
each. It was split out of `DayBoard.jsx` when adding the fourth act carried that
file to 482 lines: the seam is the one the size law forced and the right one
anyway, because below it are the acts that write and above it the geometry that
does not. `day-board-forecast.js` was split out on the same law when the measured
pre-save check carried the board back to 449, and it holds the forecast and the
rule for which prediction a save settles against. The whole keyboard moved into
`day-board-model.js` as `nudgeTarget` in the same pass, which is where the nudge
table it reads already lived. `DayBoard.jsx` is 444 lines.

`day-board-zoom.js` holds the two framings and the reveal that follows one, and
`day-chip.css` holds the chip's own rules, both split on the same law: with the
badge and the two presets added, `day-board.css` stood at 484 lines. The chip is
the seam that law would pick anyway, so its stylesheet now sits beside its
component and is imported by it. `day-board.css` is 360 and `day-chip.css` 147.

Test files, under the reserved prefix: `tests/test_p3_break_store.py`,
`tests/test_p3_break_acts.py`, `tests/test_p3_day_board.py`,
`tests/test_p3_boundary_and_regression.py`,
`tests/test_p3_direct_manipulation.py`, `tests/test_p3_save_settlement.py`,
`tests/test_p3_save_forecast.py`. **91 collected, 91 passed, 0 skipped.**

## 5. Bar 1, the stopwatch

JS-3's target is 20 s app time from opening the day to the saved move, money and
retention inside 500 ms of the drop, 0 dialogs for the move, undo in 1 keystroke,
gold from the same surface. Measured in a real browser against the running app,
viewport 1600x1000, with five other builders' servers on the same machine.

| What | Measured | Target |
|---|---|---|
| Open the day, in app, one click | **742 ms** | part of the 20 s |
| The money verdict after the drop | **51 ms** | 500 ms |
| Engine time inside that | **0.455 ms** | n/a |
| `POST /api/plan/day/score` over HTTP, 12 samples on an idle machine | **median 1.8 ms, max 2.0 ms** | 500 ms |
| the same, 10 samples with five other builders' servers running | **median 16.6 ms, max 28.8 ms** | 500 ms |
| Save one moved break | **1,132 ms** | part of the 20 s |
| Undo that save | **1,868 ms** | 1 keystroke, also a button |
| Remove a saved placement in a session that never saved it | **1,212 ms** | part of the 20 s |
| Dialogs anywhere on the path | **0** | 0 |
| Whole job, open to saved | **about 1.9 s of app time** | 20 s |

Re-measured this round on the same instance, driving the whole failure the critic
found and the route back out of it, viewport 1600x1000:

| What | Measured | Target |
|---|---|---|
| Open the day, in app, one click | **192 ms** | part of the 20 s |
| Check what saving would do, click to figures | **625 ms**, 599.5 ms of it engine | an act, not a keystroke |
| Save the moved break | **1,791 ms** | part of the 20 s |
| Remove a stranded placement after a reload | **1,172 ms** | part of the 20 s |
| Dialogs on the whole path | **0** | 0 |
| Arrow key to saved, including the optional check | **6,148 ms of wall clock** | 20 s |

Re-measured this round on the two framings, click to settled frame, same viewport:

| What | Measured | Target |
|---|---|---|
| Fit the whole day, from the maximum scale | **12 ms**, 46,800 px to 978 px | a press, not a hunt |
| Fit the selected break's programme | **20 ms**, 978 px against 978 px of track | a press, not a hunt |

Baseline today for the same job, from `06-baseline.md` section 3: the drag works
in 2,430 ms and then stops, every money figure byte identical before and after,
no undo anywhere in the product, gold on another page.

The board's own reads: `/api/plan/days` 15 ms, `/api/plan/day` 160 ms warm and
349 ms cold, `/api/breaks/{id}` 23 ms.

## 6. Bar 2, the reference mechanics

| Mechanic the reference has | Where it is here | Verified |
|---|---|---|
| Direct manipulation with no dialog | drag to move, drag the edge to resize | 0 dialogs measured through move, resize, gold, save and undo |
| A live signifier while the gesture is in flight | the snap line at the exact edge the break will land on | 1 snap line present mid-drag, gone on release |
| The object shows its own exact numbers | the chip prints what it is wide enough to print, and the badge beside it carries the rest, live through the gesture | 0 clipped labels on 80 chips, badge 01:13:55 through a real drag, section 6.1 |
| Selection produces a contextual action bar | the toolbar's selection panel: identity, start, length, gold, open | rendered on focus and on pointer down |
| The playhead position is an editable numeric field | the start clock is typed, not a readout | typed `00:45:00`, landed clamped at `00:30:44`, field restated the result |
| A duration is a typed target | the length field, Enter commits | typed and committed |
| Published nudge increments | arrow one snap unit, Shift five, Alt one second, up and down for length | printed on the surface, in both languages |
| Undo is one keystroke | Cmd Z back, Cmd Shift Z forward | chip returned to 00:28:24 |
| Undo is an object you can point at | the saved placement names the restriction that carries it, and the undo deletes exactly that one | round trip verified over HTTP |
| History does not die with the session | the inverse of a save is read off the record the server served, so it is offered on a placement saved by anyone at any time, and on one whose break the re-plan removed | measured in a fresh browser session below, 47,444.20 ILS recovered in 1,172 ms |
| Nothing is unreachable because its handle vanished | a saved placement with no chip is drawn as its own row with the same Remove control | 0 saved chips, 1 stranded row, 1 live control |
| Opening a record keeps its place in the set | the break drawer prints `1 / 80` and walks with two arrows and the up and down keys | stepped 1 / 80 to 2 / 80 |
| A figure prints its basis beside it | channel and day on every money tile, and the formula on the break | measured on the board and in the drawer |
| Fit the whole drawing, and fit the selection | the two presets beside the zoom | 46,800 px to 978 px in one press, and a 72 minute programme to 978 px in one more, section 6.1 |

### 6.1 The clipped number, and the two framings that were missing

**The defect, reproduced.** A critic measured the row above and found it false at
the scale the board opens at. Reproduced in Chrome on `רשת 13 / 2024-11-01` at
1440 x 823: the board opens at 6 px per minute, labelled 1.6x, every break in the
day is 120 s, so all 80 chips are **12 px wide**, of which 4 px is content box
once the 3 px padding either side and the 1 px border are taken. The clock
`00:28:24` wants **47 px** and the length `120s` wants **25 px**, and both were
rendered into 4 px regardless, so every chip on the track printed the character
`0` over the character `1`. **160 rendered labels, 160 of them clipped.** The
numbers only became readable at the maximum scale, 30 px per minute, where the
chip is 60 px and the day track is **46,800 px, 40.7 times the 1,150 px box that
holds it**, with no one-press way back.

**What the chip does now.** `chipLabels` in `day-board-model.js` is given the
width the chip is actually drawn at, taken from the same `positionStyle` that
places it, and the chip renders only a number it can render whole. The thresholds
are the text's own measured width plus the 8 px the padding and border take:
measured at the shipped font, 10 px Inter with tabular figures, no character of a
clock or a length needs more than 6.25 px. So a 12 px chip prints nothing, a
33 px chip prints its length, and a 58 px chip prints both. Measured after the
change, same day and same viewport: **0 numbers rendered inside a chip and 0
clipped**, and at the maximum scale both are printed inside again, which is the
ladder working at both ends rather than a label switched off.

**Where the numbers went.** Beside the object, which is where a drawing tool puts
the dimensions of a rectangle too small to hold them. The badge is drawn above
the chip and centred on it, on hover and on selection, and it is the live readout
of the gesture. Measured with one break selected and the pointer on its
neighbour: **two badges on an 80 chip board, 92 px wide against 12 px chips,
centred to the pixel, both inside the track, neither clipped**, the clock wanting
51 px in a 51 px box and the length 27 in 27. Driven through a real pointer drag,
the badge read `01:13:55` while the typed start field read `01:13:55`. The two
facts were already in the chip's accessible name and stay there, so the badge is
`aria-hidden` and takes no pointer events: a badge that swallowed a press would
break the drag it is there to narrate.

**And the way back.** Two presets sit beside the zoom, the pair a drawing tool
publishes. Measured from the maximum scale in one press each: **fit the day** put
the whole 26 hour day at **978 px against 978 px of visible track, 0 px of
horizontal scroll left over**, all 80 breaks still drawn, and the ruler stepped to
three hour labels, 9 of them, 0 overlapping; **fit the programme** put the
selected break's 72 minute programme at **978 px** and scrolled the selection back
under the eye. Timed from the click to the settled frame: **12 ms** and **20 ms**. A day is 26 hours and the shared scale's own floor of 1.9 px per
minute draws it 2,964 px wide, so a fit that could not go below the floor could
not fit the day: `clampZoom` therefore takes the floor as a parameter and a fit
lowers it to exactly the scale it measured and no further. Every other caller
passes nothing and gets the band it had, asserted by driving the shipped module:
`clampZoom(0.627)` is 1.9, `tickStep(1.9)` is 60, and the published band is still
1.9 to 30. Verified on the surface that shares the module: the week timeline's
slider still reads min 1.9, max 30, value 3.8 at 1.0x.

**What the presets cost the fold, measured rather than assumed.** Labelled in
words the pair was 192 px wide, and in the narrowest state the surface has,
English with the assistant dock open and a 730 px panel, it wrapped the toolbar
onto a second line and pushed the four commit acts from 807 px to 835 px. They
carry an icon with their words in the accessible name and the tooltip instead, at
46 px for the pair, and the same state was re-measured with the group hidden and
shown: **807 px and 835 px either way, a cost of exactly 0 px**. That state is
already 12 px past an 823 px viewport at HEAD, with or without this piece's
controls, because of the amber staleness banner another surface renders today.

**The mechanic this destination has that the reference does not.** The readout
never animates a figure that did not move. Measured with the engine's own seam on
`רשת 13 / 2024-11-01`: moving a break inside its programme changes revenue by
exactly 0.0, retention by exactly 0.0 and the objective by exactly 0.0, because a
break is priced on its programme's rating, rate, premium and length and not on
the minute it starts at. Changing its length by 60 s changes revenue by
**5,670.90 ILS**. So a horizontal drag reports the revenue as unchanged and says
why, and then shows what genuinely did change: the hour's ad load, the gap to the
neighbouring break and the compliance verdict on all six checks.

**The act that spends money is reversible from the surface that performed it,
after a reload, including when the save deletes its own chip.** This is the claim
the previous round made and did not fully hold, so both halves are measured here.

*The first half, from the round before.* The board's undo stack lives in one
browser tab, and it was once the only route back: select `003~1`, one ArrowRight,
Save, then press reload, and the settlement panel was gone, the stack empty, and
the only trace on screen an 11 px lock glyph carrying `aria-hidden`. The saved
placement, however, is a restriction and a record that both outlive the tab, so
the inverse is read off the break the payload carries rather than off the session.

*The second half, and the hole a critic measured in it.* The control that carries
that inverse hangs off the break chip, and a save is free to change how many
breaks a programme gets, which renumbers every ordinal after it. Reproduced on
`רשת 13 / 2024-11-01` at HEAD: select `2024-11-01|רשת 13|001~2`, ArrowRight once,
Save. The engine re-plans that programme from four breaks down to one, the day
falls from **1,067,845.55 to 1,020,401.35** and the count from **80 to 78**, and
the id `001~2` stops existing. Zero breaks came back carrying `saved_placement`,
no chip rendered as saved, and after a reload the board offered Undo, Discard,
Save and Remove all absent or disabled, while `data/breaks.csv` and
`data/kairos_constraints.csv` both still held the row. **47,444.20 ILS spent with
no route back from the surface that spent it.**

The record survives that, because it carries both ids the inverse needs. So the
route serves those records as `unbound_placements` and the readout draws one row
each, addressed by the record's own break id and constraint id, with the reason
the plan gives, whether the restriction is still in force, and the same Remove
control. Driven end to end in a browser, in a session that never performed the
save: 78 chips, 0 saved chips, ₪1,020,401, no settlement panel, all four readout
actions disabled, one stranded row with a live control. One click, **1,172 ms**,
and the panel printed the realised **+₪47,444**, 1,020,401 to 1,067,846, 78 breaks
to 80, with no stranded row left and 80 chips back on the board. The day is at
1,067,845.55 and both stores are byte-identical to their pre-run state.

That control has to be on screen or it is only half a route back, so its geometry
is measured too. Placed beside the actions it landed at **915 px in an 823 px
viewport**. At the top of the readout, with its note on one line, it lands at
**784 px at 1440 x 823** and **821 px at 1440 x 1000**, and the actions row it
used to sit under moved on screen at 1000 as well, from 978 px to 967 px.

**The same law then had to be applied to the commit row itself.** With one pending
edit at 1440 x 823 the panel opened at 714 px and the money at 727 px, but the
compliance verdict landed at 839 px and all four acts at 934 px, so Save needed a
scroll after every single edit, on the one row a scheduler reaches for every time.
The verdict and the four acts now open the panel and the detail that grows with the
day follows them, so where they land depends on where the panel starts and never on
how much it holds. Re-measured in a browser, one ArrowRight on the first break so
exactly one edit is pending: panel **628 px**, the four acts **637 px to 665 px**,
the verdict **642 px to 660 px**, the money tiles **673 px to 731 px**, every one of
them above the 823 px fold, the lowest control clearing it by 158 px. Re-measured
with the Hebrew labels in place, which are the ones the critic saw: the verdict is
127 px wide and the four acts 98, 108, 169 and 106 px, so the row still holds one
line and nothing moves.

**And the money a save costs is on screen before the click, not only after it.**
The readout's own change tile holds the break counts the plan already chose, which
is what lets it answer under the hand; a save writes a restriction and the engine
plans the whole day again with it in force. Measured over HTTP: pinning a break at
exactly its own offset and duration reads **₪0** on that tile and moves the day
1,067,845.55 to 1,037,270.00, and one ArrowRight on `001~2` reads **₪0** and costs
47,444.20. Nothing said so before the click. Now an unchecked edit carries the
warning in words, and `POST /api/plan/day/save-effect` replaces it with the
engine's own figures: **625 ms** in the browser, 599.5 ms of it engine, reporting
-₪47,444 and 80 breaks falling to 78 with nothing written. The save that follows
then settles **agreed** rather than diverged: "The preview said -₪47,444 and the
saved plan came back with -₪47,444. The two agree." Asserted in
`tests/test_p3_save_forecast.py` against the written save, to 0.005 on revenue,
1e-6 on retention and exactly on the break count.

Measured in a browser, in a session that never performed the save: the day stood
at ₪1,037,270 with the readout's Undo disabled and no settlement panel. Selecting
the break showed the saved placement, its restriction `aca5b82d960c` and a Remove
control. One click, 1,212 ms, and the panel printed "What the undo restored,
realised change ₪25,400, expected revenue ₪1,037,270 to ₪1,062,670, there was no
prediction to compare this against, so only the realised change of ₪25,400 is
shown". No prediction is the honest answer after a reload: there is no measured
save behind it, and zero would have been a claim.

Driven again over HTTP on the same instance for the exact figure: 1,062,669.88
to 1,026,558.30 on the save, and back to 1,062,669.88 on the inverse, a gap of
**0.0**, in 969 ms, with the placement register and the restriction store both
empty afterwards. The gold and saved states are now in the chip's `aria-label`
as well, so the two glyphs are decoration rather than the only carrier.

**The scope of a save, which is where the shipped editor is wrong.** The editor
saves a placement scoped to the whole broadcast date, and the resolver matches a
date scope against every segment on that date. Measured through the engine's own
`resolve_constraints` on 2024-11-01: the date scope binds **82 of 82 segments**;
the predicate the day board sends, date and programme and hour together on the
frozen contract, binds **1**. Driven end to end over HTTP, saving one break moved
exactly one first break and left the other 44 where the plan put them, and the
undo restored the day's totals byte for byte in 30 ms.

## 7. Bar 3, the regression row

Section 8.5's P3 row: drag and resize still work with 30 s and 60 s snap and the
zoom scale, the segment inspector still opens from the break list, and the
override preview still reports rejected overrides verbatim.

| Clause | Evidence |
|---|---|
| Drag and resize, 30 s and 60 s snap, the zoom scale | `test_the_editor_keeps_its_thirty_and_sixty_second_snap_and_the_zoom_scale`, asserted on both timelines the product now carries |
| The segment inspector opens from the break list | `test_the_segment_inspector_still_opens_from_the_break_list`. `BreakLibraryPage` keeps `ScheduleInspector` and its row click |
| The override preview reports rejected overrides verbatim | `test_the_override_preview_still_reports_rejected_overrides_verbatim`, on the live signature and source of `override_effect` |
| The whole override console keeps every capability | `test_the_override_console_keeps_every_capability_it_had`. The console moved to `OverrideDecisions.jsx` intact and is rendered under the board |
| The five product-wide honest empty states | the gold-breaks panel still answers `count: 0` with its reason; this piece adds three more states and no figures |

Nothing was deleted. The Overrides console became the second half of the day
destination rather than a page of levers you visit in order to act on a break
that lives somewhere else.

## 8. Bar 4, the laws

- **Honest math.** Every figure is the engine's own. The cheap score equals the
  expensive optimization it came from, asserted to 1e-12 on the objective and the
  retention and to 1e-6 on revenue. Three states never carry an amount.
- **A basis that could not reproduce its own figure, corrected.** The break's
  money printed the formula "rate per point times rating times length over the
  rate unit, times premium" and the programme's baseline rating. On
  `2024-11-01|רשת 13|001`, cpp 60, rating 1.7, unit 1 s, premium 0.92 and 120 s
  give 11,260.80 for all four of its breaks, while the plan credits 10,711.71,
  10,162.61, 9,613.52 and 9,064.43: over by 549.09 on the first and 2,196.37 on
  the fourth. The engine prices a break on the rating that survives once the break
  is present, so the four ratios are 1 minus k times the 0.048762 retention cost.
  The record now carries `retention_at_this_break` and `rating_at_this_break`,
  taken from the optimizer's own `_segment_retention` rather than restated, and on
  the risk-adjusted coefficient the optimizer decided with rather than the point
  estimate. Measured across all 80 breaks of the day: the worst disagreement
  between the stated basis and the credited amount was **6,072.33 ILS** before and
  **0.0065 ILS** after, the latter being the six decimals the rating publishes to.
  The drawer prints the six inputs so the arithmetic closes on screen: 60.00,
  baseline 1.700, retention 95.1238%, priced rating 1.617105, 120s / 1s, premium
  0.9200, which multiply to the ₪10,712 above them.
- **The competitor boundary.** A day plan is only ever built for
  `settings.operator_channel`, so there is no filter to forget: the segments for
  another channel are never constructed. `/api/break-operations` now applies
  `channel_scope`, measured live: 8,704 rows in, 2,540 out, 6,164 competitor rows
  excluded, 3 rival channels excluded, and only the operator's own channel in the
  payload. Two tests assert it, including one that greps the whole serialized
  payload for every other channel name.
- **The vocabulary.** ברייק, ברייקים, נעיצה, ברייקי זהב, הכנסה צפויה, עלות שימור
  are the words on the surface, and משתמש appears nowhere. The retired words fell
  from **52 to 9** in these two trees, and the 9 are named: one is the frozen HTTP
  path `/api/jobs/recompute`, and eight are the `onRecompute` and `recomputeState`
  prop names declared in the frozen `src/shell/**`, which are the shell's to
  rename and not this piece's.
- **The Israeli week.** The day picker groups ISO dates into Sunday-first weeks
  and marks Friday and Saturday as the weekend. The data stays ISO-keyed.
- **RTL.** Logical properties throughout, `dir="ltr"` on every clock, duration and
  amount so a time is never mirrored, and the drawer opens on the inline end.
- **File size.** Every owned file is inside 450 lines, asserted by a test that
  enumerates them.
- **Training versus runs.** This piece writes `data/` and `output/` and never
  `models/`. No surface it owns offers a training act, and no payload it emits
  carries a gate, a coefficient, a p-value or a drift figure.

## 9. Raised, not fixed

1. **`data/kairos_settings.json` drifts under the running product, and it is the
   competitor boundary's only input.** Measured during this round, on one machine
   in one hour: `operator_channel` went from `רשת 13` to `""` and back;
   `revenue_weight` read 35, then 60, then 85; `min_retention_floor` read 0.78,
   then 0.72, then 0.7. At HEAD the file holds `רשת 13`, 60 and 0.72. While the
   channel was blank, eight assertions in `tests/test_journey_inspector_pricing.py`
   and `tests/test_api_surface_qa.py` failed and four of this piece's own boundary
   assertions turned into silent skips. The skips are closed here by a fixture
   that declares a channel when the file carries none, with the reasoning written
   where it is defined. The drift itself belongs to whoever owns the settings
   write path. This is the wave-zero finding about a tracked file the product
   rewrites, one step worse: it is not only diff noise now, it changes engine
   figures and empties the boundary.

   Re-measured this round, one layer down: the file is an input to the day plan's
   own cache fingerprint, so a rewrite under a running suite rebuilds a day that
   nothing asked to rebuild. On one identical eight file pytest command, three
   runs failed `test_a_second_read_of_the_same_day_is_the_same_object` and three
   passed, and the fingerprint moved inside a 33 s batch,
   `918843994992144633` to `-2465893684241373906`, while an idle 35 s window
   moved nothing. The rebuild is the cache obeying its contract, so the
   assertion now reads twice with nothing between them and the suite is stable
   over three consecutive runs. The write-on-read itself is still not this
   piece's to fix.
2. **Two routes still fail the shared endpoint health sweep**, and neither is
   this piece's: `GET /api/constraints/restrictions/airings` answers 400 without a
   programme title, and `GET /api/history/since` answers 422 without a day. This
   piece's `GET /api/plan/day` was the third and is fixed: it now answers 200 with
   an honest empty state, matching `/api/plan/days`, `/api/schedule/segments` and
   `/api/gold-breaks`.
3. **`onRecompute` and `recomputeState` carry a retired word into every tree.**
   They are declared in `src/shell/TVBreakDashboard.jsx`, `top-bar.jsx`,
   `ScheduleStalenessBanner.jsx` and `workspace-router.jsx`, all frozen, and they
   reach P2's week pages and this piece's editor. Renaming them is one coordinated
   shell change, not eight local ones, so it is raised rather than done.
4. **The nav entry that opens this destination still reads Overrides.**
   `src/shell/nav.js` and `src/shell/workspace-router.jsx` froze at wave-zero
   close, so the day board reaches its door through the `Overrides` route with the
   old console rendered beneath it. The board is one click from the rail today;
   the label and the door id `plan.day` need the shell.
5. **The day's own revenue moved under a running browser, with all three
   operator stores empty.** During this round's browser measurement the same
   channel-day read 1,067,845.55 at the start of the session and 1,062,669.88
   forty minutes later, with `data/breaks.csv`, `data/kairos_constraints.csv` and
   `data/manual_overrides.csv` all at zero rows and the basis identical on both
   reads: `revenue_weight` 0.35, `risk_lambda` 0.0, 82 segments. So the move came
   from one of the other inputs the day plan fingerprints, which are `config/*.yaml`
   and `models/*`, both owned by other pieces and both being written this wave.
   It is not a defect in this piece and it did not affect any claim here, because
   every figure quoted is a difference measured inside one continuous window, and
   the exact save-and-inverse cycle was re-driven over HTTP in 969 ms and closed
   to 0.0. It is recorded because a reader comparing two numbers in this file
   taken at different hours would otherwise think one of them was wrong. It is
   the same class as item 1, one layer deeper: a shared mutable input under a live
   surface.
6. **A break id shifts when a save changes how many breaks a segment gets.**
   Identity is `segment_id + ordinal`, which is what section 5.2 specifies, so a
   segment that gains or loses a break renumbers the ones after it. **The part of
   this that was a hole under section 6's own headline is closed**: a saved record
   whose id the plan no longer has is served and reversible, measured above at
   47,444.20 ILS recovered in 1,172 ms. What remains is genuinely an identity
   nuisance rather than stranded money: a save can still change which physical
   break a different id names inside the same programme. Closing that needs an
   identity that is not ordinal, which is the same problem the owner's explicit
   break identifier solves on the traffic side.

## 10. What this piece deliberately did not build

- **The break's contents.** Section 8.2 gives them to P10, and decision 2's
  explicit per-ad identifier is what fills them. The break reports its contents as
  an honest state naming the missing input.
- **Multi-selection and a marquee.** JS-3 moves one break. Selecting several would
  need a shared action bar and a multi-break save, and neither is in the story.
  Named here so a critic reads an absence as a decision.
- **Delivered money.** The plan covers 2024-11-01 to 2024-11-30 and the one daily
  spot file covers 2025-04-27. They overlap on zero dates, so delivered is a state
  everywhere and will never be a figure until decision 4 supplies a feed.

## 11. Test-change request to C2

**Path.** `tests/test_w0_2_shell_seams.py::test_no_file_this_piece_created_is_over_the_line_cap`.

**The assertion.** `oversize == KNOWN_OVERSIZE`, where the frozen set is
`{shell/styles.css, plan/day/ScheduleEditor.jsx, rules/ConstraintBuilder.jsx}`.

**The production change that forces it.** `plan/day/ScheduleEditor.jsx` was 466
lines at wave-zero close and is 442 now, because this piece split
`schedule-editor-pin.js` out of it, so it left the set. `rules/ConstraintBuilder.jsx`
also left. Two files joined it that are not this piece's:
`kai/AssistantPanel.jsx` and `plan/week/plan-week.css`. Measured now, the set is
`{kai/AssistantPanel.jsx, plan/week/plan-week.css, shell/styles.css}`.

**Which of the two is wrong.** The frozen set, on this piece's side: a file coming
under the cap is the 450-line law being obeyed, and a test that fails for that is
a test that punishes the fix. On the other two the test is right and the
production files are over. This is one ruling covering three pieces, which is why
it goes to C2 rather than being edited by any of them.

**Re-measured at the close of this round.** The other pieces have since brought
their two files under the cap as well, so the measured set is now
`{shell/styles.css}` alone and the assertion fails naming
`plan/day/ScheduleEditor.jsx` and `rules/ConstraintBuilder.jsx` as missing. The
ruling is now a one-line edit: the frozen set is `{shell/styles.css}`. Nothing in
this piece's two trees is over the cap, asserted by
`test_every_source_file_this_piece_owns_is_inside_the_size_law`, which is what
forced the `day-board-writes.js` split in section 4.
