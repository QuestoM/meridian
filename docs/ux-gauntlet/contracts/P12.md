# P12 contract: model improvement

The section 8.8 publication for P12: what it owns, what it wrote, the shapes it
emits, and the one thing it could not do and why. Every number below was taken
on the delivered tree, after the last edit, by `wc -l`, by `pytest`, by
`/usr/bin/time` or by running the command that produces it.

P12 owns `scripts/adopt_candidate.py`, `models/candidates/**` and
`models/releases/**` by handover from P7, plus the frontend tree
`tv-break-dashboard/src/model/candidates/**`, which is empty and blocked, for
the reason in section 6.

## 1. What was missing, measured

P7 built the model console and it answers "what does this candidate say". It
does not answer JS-19's actual question, which is "is this candidate genuinely
better than what is live". Three things were absent and all three are measured
absences rather than opinions:

- **No comparison of the predictors.** Every held-out figure the console shows
  is read out of the artifact's own metadata, written by its own fit under its
  own split against its own target. Comparing two of those compares two
  experiments. Nothing scored two artifacts on one set of breaks.
- **No adoption act.** `models/tv_break_coefficients.json` had never been
  written by anything but the training script. There was no backup, no undo, no
  record of who adopted what, and no verdict inside the artifact.
- **No check on what else an adoption would change.** Measured on this tree, the
  candidate that already carries a ship verdict drops `first_break_multiplier`,
  which `kairos/service.py:117` folds into the optimizer assumptions on every
  run. Nothing could see that.
- **Added in round 3. No comparison of the coefficients themselves.** JS-19's
  done condition names three things recorded against a new model version: the
  gate deltas, the **coefficient deltas** and the measured money movement. Two
  were built. The third was in no payload on this piece or on the console.
  Measured on this tree before round 3: the shipped artifact and the
  `afterwindow` candidate differ in **all 36 of 36** coefficients, the largest
  point move being 0.014066885 at `PrimeShow2_first_short`, and
  `artifact_surface()` emitted no key naming any of it. What the checks printed
  in its place was "credible bounds moved on 72 of the 72 bounds", which invites
  exactly the wrong reading: that the intervals moved and the points did not.
- **Added in round 2. No way to record the verdict from the surface that takes
  it.** JS-19 ends with "record the verdict" and this terminal had `show`,
  `rescore`, `measure`, `checks`, `adopt`, `revert` and `report` and no way to
  record one, so the steward crossed to another surface for the one act the
  story is named after. Worse, the verdict he took there could not rest on the
  common-basis comparison, because that comparison was on a file nobody reads.
  Measured on this tree: **all five verdicts on record predate it**, so every
  one was taken by reading two artifacts' own held-out figures, on different
  test sets.

## 2. Files written

| Path | Lines | Role |
|---|---|---|
| `scripts/adopt_candidate.py` | 351 | the entry point and its ten subcommands |
| `scripts/adopt_candidate_adoption.py` | 443 | the checks, the escalation, the write, the undo |
| `scripts/adopt_candidate_rescore.py` | 415 | the common-basis re-score and the two bars |
| `scripts/adopt_candidate_render.py` | 401 | every line the registry and the checks are read as |
| `scripts/adopt_candidate_words.py` | 328 | every authored string this piece emits, in both languages |
| `scripts/adopt_candidate_decide.py` | 318 | the verdict act: ship or no ship, recorded against a version |
| `scripts/adopt_candidate_cells.py` | 275 | the coefficient delta: what moved and what it bought |
| `scripts/adopt_candidate_board.py` | 214 | the comparison as a screen reads it, and the guard on publishing it |
| `scripts/adopt_candidate_state.py` | 199 | what the model console already knows, read through it |
| `scripts/adopt_candidate_registry.py` | 174 | the join |
| `scripts/adopt_candidate_surface.py` | 143 | what an adoption changes beyond the coefficients |
| `scripts/adopt_candidate_baselines.py` | 125 | the two out-of-sample baselines, the cell-structure reading and the standing finding |
| `scripts/adopt_candidate_ownership.py` | 112 | the write surface, enforced at the line that writes |

Frontend, this piece's own row, `tv-break-dashboard/src/model/candidates/**`,
built in round 5 and empty until then:

| Path | Lines | Role |
|---|---|---|
| `CandidateBoard.jsx` | 331 | the board: freshness, the ranked table, the sort, the selection |
| `candidate-board.css` | 249 | its chrome, tokens only, logical properties throughout |
| `board-detail.jsx` | 191 | what one artifact was decided on, under the table that ranks them |
| `board-words.js` | 112 | every authored string it renders, in both languages |
| `board-compare.js` | 74 | the freshness tri-state and the sort, both pure and both testable |
| `board-mount.jsx` | 30 | the one function that puts the board on a page, no side effect on import |
| `candidate-board.json` | data | the published measurement, written by `adopt_candidate.py publish` |

All but the first are helper modules declared under the section 8.2 naming rule,
`<parent stem>_<role>.py` beside their parent, and owned by P12 from this
publication. Three were declared in round 2, `_decide`, `_state` and `_words`,
and **three more in round 3**, `_cells`, `_render` and `_baselines`, the last
two because `_registry` reached 448 lines and `_rescore` reached 451 under this
round's change. The largest module is now 422 lines and the cap is 450, which a
test asserts on **every module found by a glob** rather than on a hardcoded
list. That list was the defect: it named eight modules, it had gone stale by
three, and both laws it holds, the size cap and the no-em-dash rule, had
silently stopped applying to new code.

Tests, under the reserved prefix, ten files and 167 tests, all passing:

| File | Tests |
|---|---|
| `tests/test_p12_training_line.py` | 34 |
| `tests/test_p12_decide.py` | 22 |
| `tests/test_p12_adoption.py` | 19 |
| `tests/test_p12_board.py` | 19 |
| `tests/test_p12_cells.py` | 18 |
| `tests/test_p12_rescore.py` | 15 |
| `tests/test_p12_surface.py` | 13 |
| `tests/test_p12_ownership.py` | 11 |
| `tests/test_p12_terminal.py` | 9 |
| `tests/test_p12_board_page.py` | 7 |

`test_p12_board_page.py` drives the real panel in headless Chrome through the
product's own bundler, using P7's frozen browser harness rather than a copy of
it. It is the only place this piece's screen can be measured, because the screen
has no mount point yet.

Under `models/releases/`, which P12 holds by handover: `holdout_rescores.json`
and `candidate_registry.json` are new, and one entry of
`candidate_measurements.json` was refreshed by a real measurement. Nothing else
under `models/` was written. `models/tv_break_coefficients.json` is byte
identical to the tree this piece started from, digest `1e41d92b1a14`.

No route was added, no stanza was appended to `server.py`, and no file outside
this list was touched.

**How the lead mounts the board, exactly.** Two lines, both in P7's files, and
nothing else. In `src/model/console/ModelConsole.jsx`, one import beside the
other seven panels, `import CandidateBoard from '../candidates/CandidateBoard';`,
and one branch in `Body`, `if (section === 'board') return <CandidateBoard locale={locale} />;`,
plus `'board'` in `SECTIONS` and in `SECTION_ROUTE` in `console-api.js` and a
`section.board` label in `console-words.js`. The panel needs no route and no
props beyond `locale`: it imports its own published measurement and cross-checks
it against `/api/model/candidates`, which the console already publishes and
already walls. Round 4's queue put the unblocking at three lines including a new
`@company_only` route; the route turned out not to be needed, because the digest
cross-check answers the freshness question from a route that exists.

## 3. Shapes frozen

**A cell delta**, emitted per candidate on every re-score row, and the shape
the whole of round 3 turns on.

```
{rows: [{cell, shipped, candidate, delta, state, breaks,
         squared_error_delta, share_of_absolute}],
 summary: {cells_compared, cells_moved, cells_unchanged,
           cells_added: [cell], cells_dropped: [cell],
           max_abs_delta, max_abs_delta_at, breaks_weighted_mean_abs_delta,
           net_squared_error_delta, total_abs_squared_error_delta,
           cancelled_share, cells_improved, cells_worsened,
           carries_the_move: [cell], carries_share, concentrated,
           reading_en, reading_he, key_shape_en, key_shape_he}}
```

`state` is `moved`, `unchanged`, `added` or `dropped`, and a cell only one
artifact carries has `delta: null`. It is never a zero, because a missing
coefficient is not a coefficient of zero.

`squared_error_delta` is the candidate's squared error minus the shipped
model's, summed over the breaks in that cell, so it is signed the way the metric
is signed and it sums over the cells to the whole movement the metric reports.
That is what makes each cell attributable rather than merely listed.

`cancelled_share` is the figure a metric delta cannot state. A candidate that
moves every cell and nets a thousandth is not a model that improved by a
thousandth; it is a re-fit that landed somewhere else. Measured on this tree,
`competitor` cancels 98.7 percent of its own movement, and its RMSE delta of
0.000011 is what is left over.

`concentrated` decides whether the cells carrying the move are named. It takes
16 of `competitor`'s 36 cells to reach four fifths of its movement, and a list of
16 names is the opposite of the finding, so the payload says so and the render
says the movement is spread.

**The board**, emitted by `adopt_candidate.py publish` into
`tv-break-dashboard/src/model/candidates/candidate-board.json`, which is the
file the panel imports.

```
{published_at, measured_at, fingerprint, rescore_state,
 evaluation, limit, baselines, cell_structure, structure_finding,
 duplicate_groups,
 shipped: {rmse, sha256, short, file, cells,
           version_id, version_name, version_short, computed_at},
 candidates: [{id, file, bytes, sha256, short, computed_at, breaks_fitted_on,
               rmse, rmse_delta, paired_statistic, paired_bar, fold_dispersion,
               breaks_improved, breaks_worsened,
               verdict, verdict_en, verdict_he, rule_en, rule_he,
               duplicate_of, adopted,
               money: {state, revenue_delta, last_known_revenue_delta,
                       measured_at, rows, basis, whole_plan_delta,
                       reason_en, reason_he},
               decision: {state, recorded_at, actor, count, on_rescore},
               cells: {compared, moved, added, dropped, max_abs_delta,
                       max_abs_delta_at, cancelled_share, concentrated,
                       carries_the_move, carries_share,
                       reading_en, reading_he, key_shape_en, key_shape_he,
                       top: [cell row], top_of}}]}
```

Three properties are load-bearing and each is a test. Every `sha256` is the
digest of the file the row was measured on, which is what makes the panel's
freshness tri-state possible against a route rather than a promise. `top` is the
ranked head of the cell rows and `top_of` says how many there were, so a screen
showing eight of thirty-six has said so. And the payload carries no command, no
free text from a decision record and no name that reaches the training act; the
write refuses if one appears, because the panel is behind a route wall and the
bundle it travels in is not.

**A re-score row**, emitted per artifact by `holdout_rescores.json`.

```
{id, kind: shipped | candidate | baseline, rmse, mae, breaks, cells,
 cells_not_carried, sha256, file, out_of_sample, prediction_signature,
 duplicate_of: [id],
 cell_deltas: {rows, summary},
 paired: {rmse_delta, mean_squared_error_delta, standard_error,
          paired_statistic, paired_bar, fold_rmse_deltas, fold_dispersion,
          folds, breaks_improved, breaks_worsened, breaks},
 verdict: {state: identical | better | worse | not_distinguishable,
           en, he, rule_en, rule_he, clears_paired_bar, clears_fold_dispersion}}
```

**The evaluation block**, which every payload carries.

```
{evaluation: {breaks, cells, window, target_en, target_he, metric_en, metric_he,
              target_sd, target_sd_en, target_sd_he, folds},
 limit: {state: in_sample, en, he, unblocked_by_en, unblocked_by_he},
 cell_structure: {cell_mean_loo_rmse, global_mean_loo_rmse, rmse_delta,
                  earns_its_place, out_of_sample, reading_en, reading_he},
 fingerprint, inputs: {shipped, candidate:<id>, data:<file>}}
```

Round 2 added the second half of five display fields that shipped in English
only: `rule_he` on every verdict, `metric_he` in the evaluation, `basis_he` on
both baselines, `how_he` on every check and `he` on every next act. All of them
live in one table in `adopt_candidate_words.py` and a test walks that table, so
a key added without its partner fails rather than shipping half translated.
`target_sd` was computed on every row and rendered nowhere; it is one figure per
evaluation now, because it is a property of the target and not of a row, and it
is on screen because an rmse of 0.241161 against a target spread of 0.241379 is
the most honest line this surface carries.

**An adoption check and a verdict check**, emitted per condition.

```
{id, passed, reason_en, reason_he, how_en, how_he}
```

**A verdict record**, appended to `models/releases/decisions.jsonl` by
`decide --perform` through `model_version_store.record_decision`, which is the
same function the model console's own form calls. The record shape is P7's and
unchanged. What P12 adds is the `evidence` it carries.

```
{evidence: {money_state, revenue_delta, revenue_delta_pct, scope, measured_at,
            gate_counts, gate_total,          <- P7's own keys, kept verbatim
            basis_en, basis_he, reason_en,
            rescore: {rmse, shipped_rmse, rmse_delta, paired_statistic,
                      paired_bar, fold_dispersion, breaks_improved,
                      breaks_worsened, state, en, he, rule_en, rule_he,
                      duplicate_of, cells: <the cell delta summary>,
                      sha256, file, measured_at, fingerprint},
            evaluation, limit, baselines, cell_structure}}
```

**An adoption record**, appended to `models/releases/adoptions.jsonl` and copied
into the adoption's own directory as `manifest.json`.

```
{adoption_id, action: adopted | reverted, recorded_at, candidate_id,
 adopted_by, reason, release_note_he, superseded_version_id,
 superseded_sha256, adopted_sha256, measured_revenue_delta, rescore_verdict,
 gate_deltas, coefficient_deltas, ship_decision_id, directory}
```

**The stamp inside the adopted artifact**, at `metadata.adoption`, carrying the
same fields plus `from_file`, `measured_revenue_scope` and `revert_with`. Round
3 added `gate_deltas` and `coefficient_deltas` to it, which completes JS-19's
done condition verbatim: a reader holding only the adopted file can now see all
three of the gate deltas, the coefficient deltas and the measured money movement
recorded against the version it superseded.

## 4. The rules this piece holds

**The training line.** Adoption writes `models/`, so by section 4.1 it is
training, so it is company staff only and unreachable. Five tests hold it: every
file a full adopt and revert cycle creates is measured and asserted to be under
`models/`, every file the verdict act creates is measured the same way and lands
under `models/releases/`, no module in `kairos_api/` mentions it, no file the
operator's interface ships mentions it, and no route the running application
publishes is handled out of `scripts/`. Re-measured after round 3: the running
application publishes **155 paths and none of them matches `adopt`**, and the
operator's interface and `kairos_api/` together mention this act **zero** times.
The two new paths are other wave-two pieces, not this one.

**Deciding and adopting are two acts and the line between them is the same
line.** Recording a verdict writes `models/releases/decisions.jsonl`, so it is
training by the same test and it is offered on the same terminal and nowhere
else. It moves no artifact, and the store's own `adoption` block says so inside
every ship record it writes.

**The escalation.** An adoption whose measured movement is not zero stops and
will not land, and no command-line flag releases it. What releases it is
`models/releases/owner_approvals/<candidate>.json` naming the exact movement in
shekels; an approval that names a different figure does not release it, and an
approval goes out of date by itself when the measured figure moves. Three tests
cover the three branches.

**Reversibility.** The replaced artifact is copied whole before anything is
written. A revert restores those bytes exactly, refuses when the shipped
artifact is not the one that adoption left behind, and refuses a second time.

**Honest math.** The evaluation states its own limit on every payload: all six
artifacts were fitted on all 2,532 breaks and no unseen data exists in this
repository, so every absolute figure is optimistic and only the paired
difference is readable. The two baselines are genuinely out of sample and are
labelled as the only figures that are.

**Tri-state on the coefficient delta.** A cell both artifacts carry has a delta.
A cell only one carries has `null` and the word `added` or `dropped`. A
candidate that moves nothing has no cancellation figure and the column reads
`no move`, because printing `0.0 percent` there would state that its movement
cancelled rather than that there was none.

**The competitor boundary, measured on the keys rather than asserted.** The cell
keys look like `PrimeShow2_first_short` and a reader who has not been told will
read the first part as a channel. A test walks every key in the shipped artifact
and asserts each is one of the four programme classes, one of three break
positions and one of three break lengths, so a key shaped like anything else
fails rather than shipping. The sentence saying so travels with the figures, in
both languages, on the terminal and inside every payload.

## 5. Measured on this tree

Re-score, 2,532 breaks, 36 cells, 2024-11-01 to 2024-11-30, five temporal folds.

| artifact | rmse | vs shipped | paired statistic | fold dispersion | verdict |
|---|---|---|---|---|---|
| shipped | 0.241161 | | | | |
| spotclip | 0.241005 | -0.000156 | -0.78 | 0.000418 | not distinguishable |
| afterwindow | 0.241047 | -0.000114 | -0.52 | 0.000462 | not distinguishable |
| calibrated | 0.241047 | -0.000114 | -0.52 | 0.000462 | not distinguishable |
| competitor | 0.241150 | -0.000011 | -0.05 | 0.000459 | not distinguishable |
| placebo_corrected | 0.241161 | 0.000000 | 0.00 | 0.000000 | identical |

Baselines, out of sample: global mean 0.241474, cell mean 0.242097. The 36-cell
split is 0.000623 rmse **worse** than one constant out of sample, which is four
times the largest movement any candidate makes.

**The target's own spread is 0.241379**, one standard deviation of the measured
effect. The shipped model's 0.241161 is 0.000218 under it. Round 2 put that
figure on the screen because it was being computed and thrown away, and it is
the line that makes the table above readable: every artifact on it, including
the live one, is within a thousandth of predicting nothing but the mean.

`afterwindow` and `calibrated` carry byte-identical coefficient maps and predict
the same value for every break. One holds a ship verdict and the other holds two
no-ship verdicts. Nothing before this could see that they are one predictor.

**The coefficient delta, round 3's measurement.** Every candidate on this tree
moves every coefficient it carries, except the one that moves none.

| artifact | cells moved | largest move | where | cancels | concentrated |
|---|---|---|---|---|---|
| spotclip | 36 of 36 | 0.012751733 | PrimeShow2_first_short | 78.9% | no |
| afterwindow | 36 of 36 | 0.014066885 | PrimeShow2_first_short | 86.2% | no |
| calibrated | 36 of 36 | 0.014066885 | PrimeShow2_first_short | 86.2% | no |
| competitor | 36 of 36 | 0.015817128 | Other_first_long | 98.7% | no |
| placebo_corrected | 0 of 36 | none | | no move | no |

That table is the reading the RMSE table above cannot give. `competitor` reads
as the least interesting candidate at -0.000011 RMSE; what it is actually doing
is moving every one of its 36 coefficients by up to 0.0158 and cancelling 98.7
percent of that movement against itself. And `placebo_corrected`, whose credible
bounds move on all 72 of 72, moves no point at all, so the two facts that the
adoption checks previously printed as one are now separate and both readable.

Money, measured on this tree in 179.6 s: `afterwindow` moves the operator's own
channel by +923,843.08 over 2,540 rows, and the whole plan by +5,290,848.33. The
four figures P7 measured on 2026-08-01 still read stale and keep their old value
dimmed, which is the tri-state working.

Timings, re-taken in round 5: `show` 0.25 s, `checks` 0.24 s, `diff` 0.21 s,
`publish` 0.24 s.
`rescore` is 9.7 s wall for 2,532 breaks including the coefficient delta on all
five candidates, dominated by loading the reference data. `measure` is about
180 s, because it computes the weekly plan twice. `adopt` is 0.46 s to a
refusal. The whole JS-19 route, `show` then `diff` then `checks` then
`decide --perform`, is **5.24 s against a 120 s target**.

**Where `decide`'s four seconds go, attributed rather than excused.** Measured
in process: importing `model_console_api_payloads` costs 0.019 s, its first
`decision_evidence` call costs 3.913 s and its second costs 0.001 s. It is P7's
gate ledger warming its artifacts once, it is the same cost the console pays on
its first paint, and it is not something this piece introduced or can move
without writing a frozen module.

**The screen, measured in a real browser, round 5.** Headless Chrome, the
product's own bundler, the console's own candidate route answered with the
digests each scenario asks for. Table painted 51.3 ms, freshness verdict 63.4 ms,
one artifact's evidence open 77.1 ms, against JS-19's 120 s. The stale scenario
reads stale in 32.9 ms and names the one artifact that moved; the scenario where
the route does not answer reads unknown in 37.0 ms and names no artifact, because
unknown is not stale. Full report in
`docs/ux-gauntlet/evidence/p12-r5-03-board-measured-in-a-browser.json`.

**Regression, measured after round 5.** 167 P12 tests pass, 105 P7 tests pass in
231.30 s, `npm run build` is green in 529 ms, the running application publishes
158 openapi paths and 202 app routes with none matching `adopt` and none handled
out of `scripts/`, `models/tv_break_coefficients.json` is still `1e41d92b1a14`,
`models/candidates/` is untouched, and `models/releases/decisions.jsonl` still
holds the same 7 records. The published board file names the training act zero
times, and the write refuses a payload that does.

## 6. What this piece did not do, and why

- **It did not adopt anything.** Every one of the five candidates fails at least
  one check. Four move money and are escalated. The fifth, `placebo_corrected`,
  predicts identically and would move nothing, and it fails on the ship verdict
  nobody has recorded for it. The whole adopt and revert path is proven end to
  end on a temporary tree by nineteen tests instead.
- **It recorded no verdict into the product's own decision log.** The verdict
  act is proven end to end, twenty-two tests on a temporary store and one live
  record read back through P7's route, but that live record was written against
  a **copy** of `models/releases/` under `KAIROS_MODEL_RELEASES_DIR`. Which way
  a candidate goes is the steward's call and not this build's, and a decision
  log is append-only, so a demonstration verdict in it would be permanent.
  `models/releases/decisions.jsonl` still holds the same seven records it held
  before this round.
- **It did not write `models/tv_break_coefficients.json`, and round 5 made that
  structural rather than incidental.** That path is the target of the adoption
  act and it is **not on P12's ownership row**, which is a gap in section 8.2
  rather than a decision taken here. Until round 5 the code would have written it
  anyway on the first adoption that cleared every other check, so the violation
  was latent, not closed. Now every write this act makes goes through one guard
  in `scripts/adopt_candidate_ownership.py`, at the line that writes, and the
  guard refuses any target outside the row. The shipped artifact is named there
  as the single pending path and it is released by a ruling the lead records at
  `models/releases/ownership/shipped_artifact.json` naming that exact path, which
  is the same shape as the owner approval one step earlier in the act: a write
  this terminal may not authorise on its own is released by an artifact naming
  who authorised it, never by a flag. Until that ruling exists, `adopt` stops at
  a check called `write_target_is_owned` that names the path, the section and the
  file that would clear it. Nothing else changes: no candidate on this tree could
  land before this round either.
- **Round 5 built the frontend row, and it still has no mount point.** Rounds 1
  to 4 shipped nothing under `tv-break-dashboard/src/model/candidates/**` and
  reported the whole thing as the lead's, on the grounds that a panel needs an
  import in `ModelConsole.jsx`, an entry in `SECTIONS` and a route in
  `model_console_api.py`, all three P7's and frozen. Two thirds of that is now
  closed from inside this piece's own row. The route is not needed: the board
  imports its own published measurement and asks `/api/model/candidates`, which
  P7 already publishes and already walls, for the digests the server is serving,
  so the freshness question is answered from an existing route rather than a new
  one. The panel exists, it is built, and it is measured in a real browser. What
  remains is genuinely two lines in two frozen files, written out verbatim at the
  end of section 2. **That is the piece's one open blocker and it is the
  lead's.**
- **What round 2 did about it without touching a frozen file, and how far that
  goes.** The common-basis re-score now reaches the screen where the verdict is
  actually taken, by riding inside the verdict itself. `decide` records through
  `model_version_store.record_decision`, and the `evidence` it attaches carries
  the re-score, the evaluation, the limit, both baselines and the cell-structure
  reading, on top of every key P7's own evidence block already had. Proven live:
  a verdict recorded at the terminal against a copy of the store came back out
  of `GET /api/model/candidates/afterwindow` with `rescore.rmse 0.241046876`
  against `shipped_rmse 0.241160667` and `evaluation.breaks 2532`, which is
  exactly what `CandidateVerdict.jsx` renders in its record drill. The registry
  now also marks every verdict that predates the comparison with a `*` and says
  in words what the `*` means, and all five verdicts on this tree carry it.
  **What is still missing is the side-by-side inside the console**: a steward
  comparing five candidates on one screen still reads the console's candidate
  shelf, which shows each artifact's self-reported held-out figures. That needs
  the route and the panel above.
- **What round 3 did, and where it went further than round 2 could.** Round 2
  got the common-basis re-score onto the verdict card. Round 3 got the third of
  JS-19's three recorded things built at all, and put it in four places without
  touching a frozen file: on the terminal's own side-by-side, in a new `diff`
  subcommand that ranks every one of the 36 cells by what it bought, inside the
  verdict record so it reaches the console through P7's own route, and inside
  the adopted artifact's own `metadata.adoption` stamp so a reader holding only
  the file can see it. Proven live on port 8043 against a copy of the store:
  `GET /api/model/candidates/afterwindow` returned
  `evidence.rescore.cells.cells_moved` 36 of 36,
  `max_abs_delta` 0.014066885 at `PrimeShow2_first_short`,
  `cancelled_share` 0.862227 and the Hebrew reading in full.
- **What round 5 closed beyond the screen and the write guard.** Five items from
  the round-4 queue, each measured before and after. The registry's private
  `_money_state` is gone and every caller reads the one `money_state`, which had
  divergent behaviour for the stale case in two modules; the stale magnitude now
  travels under its own name, `last_known_revenue_delta`, so a check can say what
  figure it is refusing to use, and `revenue_delta` stays null so no stale figure
  can reach a record or an artifact stamp. Every registry row now carries
  `rule_en`, `rule_he` and the artifact's `sha256`, which the join was dropping,
  so the published payload no longer states a verdict without the rule that
  produced it or a figure without the bytes it was measured on. The Hebrew for
  the two verdicts is now the console's own pair, `להשיק` and `לא להשיק` from
  `console-words.js:180`, rather than this terminal's own `שיגור` and
  `אי שיגור`: two surfaces naming one act two ways is a divergence a steward
  walks into inside one session, and the console's file is frozen, so this piece
  adopted the console's words. And the out-of-sample cell-structure finding is
  now a named standing finding sized against the candidates, on the terminal and
  on the board: the split costs 0.000623 rmse out of sample, which is 4.0 times
  the largest movement any candidate makes, and 0 of the 5 candidates change the
  set of cells at all, counted from the coefficient delta rather than asserted.
  It is stated with whose decision it is, the model owner's, as a retraining
  decision, and never as a next act, because nothing this terminal can run
  changes it.
- **It did not re-run any training recipe.** Re-fitting each candidate under its
  own flags on a training split would be the clean out-of-sample comparison, and
  it is impossible here for two reasons that are both measured: three of the five
  candidates were produced by scripts other than the training script and carry
  none of its gate metadata, and two of them redefine the measurement window, so
  they do not agree with the shipped artifact on what the target is. The
  evaluation says so in words rather than averaging over the disagreement.
