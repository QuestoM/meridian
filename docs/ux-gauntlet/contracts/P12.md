# P12 contract: model improvement

The section 8.8 publication for P12: what it owns, what it wrote, the shapes it
emits, and the one thing it could not do and why. Every number below was taken
on the delivered tree, after the last edit, by `wc -l`, by `pytest`, by
`/usr/bin/time` or by running the command that produces it.

P12 owns `scripts/adopt_candidate.py`, `models/candidates/**` and
`models/releases/**` by handover from P7, plus the frontend tree
`tv-break-dashboard/src/model/candidates/**`, which is empty and blocked, for
the reason in section 6.

## 1. What was missing, measured

P7 built the model console and it answers "what does this candidate say". It
does not answer JS-19's actual question, which is "is this candidate genuinely
better than what is live". Three things were absent and all three are measured
absences rather than opinions:

- **No comparison of the predictors.** Every held-out figure the console shows
  is read out of the artifact's own metadata, written by its own fit under its
  own split against its own target. Comparing two of those compares two
  experiments. Nothing scored two artifacts on one set of breaks.
- **No adoption act.** `models/tv_break_coefficients.json` had never been
  written by anything but the training script. There was no backup, no undo, no
  record of who adopted what, and no verdict inside the artifact.
- **No check on what else an adoption would change.** Measured on this tree, the
  candidate that already carries a ship verdict drops `first_break_multiplier`,
  which `kairos/service.py:117` folds into the optimizer assumptions on every
  run. Nothing could see that.

## 2. Files written

| Path | Lines | Role |
|---|---|---|
| `scripts/adopt_candidate.py` | 236 | the entry point and its seven subcommands |
| `scripts/adopt_candidate_rescore.py` | 437 | the common-basis re-score, the baselines, the two bars |
| `scripts/adopt_candidate_registry.py` | 310 | the join and the terminal render |
| `scripts/adopt_candidate_adoption.py` | 435 | the checks, the escalation, the write, the undo |
| `scripts/adopt_candidate_surface.py` | 109 | what an adoption changes beyond the coefficients |

The last four are helper modules declared under the section 8.2 naming rule,
`<parent stem>_<role>.py` beside their parent, and owned by P12 from this
publication. The largest is 437 lines and the cap is 450, which a test asserts.

Tests, under the reserved prefix, four files and 62 tests, all passing:

| File | Tests |
|---|---|
| `tests/test_p12_rescore.py` | 15 |
| `tests/test_p12_adoption.py` | 18 |
| `tests/test_p12_surface.py` | 13 |
| `tests/test_p12_training_line.py` | 16 |

Under `models/releases/`, which P12 holds by handover: `holdout_rescores.json`
and `candidate_registry.json` are new, and one entry of
`candidate_measurements.json` was refreshed by a real measurement. Nothing else
under `models/` was written. `models/tv_break_coefficients.json` is byte
identical to the tree this piece started from, digest `1e41d92b1a14`.

No route was added, no stanza was appended to `server.py`, and no file outside
this list was touched.

## 3. Shapes frozen

**A re-score row**, emitted per artifact by `holdout_rescores.json`.

```
{id, kind: shipped | candidate | baseline, rmse, mae, breaks, cells,
 cells_not_carried, sha256, file, out_of_sample, prediction_signature,
 duplicate_of: [id],
 paired: {rmse_delta, mean_squared_error_delta, standard_error,
          paired_statistic, paired_bar, fold_rmse_deltas, fold_dispersion,
          folds, breaks_improved, breaks_worsened, breaks},
 verdict: {state: identical | better | worse | not_distinguishable,
           en, he, rule_en, clears_paired_bar, clears_fold_dispersion}}
```

**The evaluation block**, which every payload carries.

```
{evaluation: {breaks, cells, window, target_en, target_he, metric_en, folds},
 limit: {state: in_sample, en, he, unblocked_by_en, unblocked_by_he},
 cell_structure: {cell_mean_loo_rmse, global_mean_loo_rmse, rmse_delta,
                  earns_its_place, out_of_sample, reading_en, reading_he},
 fingerprint, inputs: {shipped, candidate:<id>, data:<file>}}
```

**An adoption check**, emitted per condition.

```
{id, passed, reason_en, reason_he, how_en}
```

**An adoption record**, appended to `models/releases/adoptions.jsonl` and copied
into the adoption's own directory as `manifest.json`.

```
{adoption_id, action: adopted | reverted, recorded_at, candidate_id,
 adopted_by, reason, release_note_he, superseded_version_id,
 superseded_sha256, adopted_sha256, measured_revenue_delta, rescore_verdict,
 ship_decision_id, directory}
```

**The stamp inside the adopted artifact**, at `metadata.adoption`, carrying the
same fields plus `from_file`, `measured_revenue_scope` and `revert_with`.

## 4. The rules this piece holds

**The training line.** Adoption writes `models/`, so by section 4.1 it is
training, so it is company staff only and unreachable. Four tests hold it: every
file a full adopt and revert cycle creates is measured and asserted to be under
`models/`, no module in `kairos_api/` mentions it, no file the operator's
interface ships mentions it, and no route the running application publishes is
handled out of `scripts/`.

**The escalation.** An adoption whose measured movement is not zero stops and
will not land, and no command-line flag releases it. What releases it is
`models/releases/owner_approvals/<candidate>.json` naming the exact movement in
shekels; an approval that names a different figure does not release it, and an
approval goes out of date by itself when the measured figure moves. Three tests
cover the three branches.

**Reversibility.** The replaced artifact is copied whole before anything is
written. A revert restores those bytes exactly, refuses when the shipped
artifact is not the one that adoption left behind, and refuses a second time.

**Honest math.** The evaluation states its own limit on every payload: all six
artifacts were fitted on all 2,532 breaks and no unseen data exists in this
repository, so every absolute figure is optimistic and only the paired
difference is readable. The two baselines are genuinely out of sample and are
labelled as the only figures that are.

## 5. Measured on this tree

Re-score, 2,532 breaks, 36 cells, 2024-11-01 to 2024-11-30, five temporal folds.

| artifact | rmse | vs shipped | paired statistic | fold dispersion | verdict |
|---|---|---|---|---|---|
| shipped | 0.241161 | | | | |
| spotclip | 0.241005 | -0.000156 | -0.78 | 0.000418 | not distinguishable |
| afterwindow | 0.241047 | -0.000114 | -0.52 | 0.000462 | not distinguishable |
| calibrated | 0.241047 | -0.000114 | -0.52 | 0.000462 | not distinguishable |
| competitor | 0.241150 | -0.000011 | -0.05 | 0.000459 | not distinguishable |
| placebo_corrected | 0.241161 | 0.000000 | 0.00 | 0.000000 | identical |

Baselines, out of sample: global mean 0.241474, cell mean 0.242097. The 36-cell
split is 0.000623 rmse **worse** than one constant out of sample, which is four
times the largest movement any candidate makes.

`afterwindow` and `calibrated` carry byte-identical coefficient maps and predict
the same value for every break. One holds a ship verdict and the other holds two
no-ship verdicts. Nothing before this could see that they are one predictor.

Money, measured on this tree in 179.6 s: `afterwindow` moves the operator's own
channel by +923,843.08 over 2,540 rows, and the whole plan by +5,290,848.33. The
four figures P7 measured on 2026-08-01 still read stale and keep their old value
dimmed, which is the tri-state working.

Timings: `show` 0.31 s, `checks` 0.19 s, `report` 0.20 s, `adopt` 0.46 s to a
refusal. `rescore` is about 10 s, dominated by loading the reference data.
`measure` is about 180 s, because it computes the weekly plan twice.

## 6. What this piece did not do, and why

- **It did not adopt anything.** Every one of the five candidates fails at least
  one check. Four move money and are escalated. The fifth, `placebo_corrected`,
  predicts identically and would move nothing, and it fails on the ship verdict
  nobody has recorded for it. The whole adopt and revert path is proven end to
  end on a temporary tree by eighteen tests instead.
- **It did not write `models/tv_break_coefficients.json`.** That path is the
  target of the adoption act and it is **not on P12's ownership row**, which is
  a genuine gap in section 8.2 rather than a decision taken here. Nothing is
  blocked by it today, because no adoption can land anyway, but the piece that
  owns the act should own the artifact it adopts.
- **It shipped nothing under `tv-break-dashboard/src/model/candidates/**`,
  which is its own frontend row.** That tree cannot be reached. Mounting a panel
  requires an import in `src/model/console/ModelConsole.jsx` and a section in
  `src/model/console/console-api.js`, both P7's and both frozen, and feeding it
  requires a route in `kairos_api/model_console_api.py`, also P7's. There is no
  dynamic panel registry and no `import.meta.glob` anywhere in the frontend, so
  there is no seam. **This is the piece's one open blocker and it is the lead's.**
  What ships instead is the exact payload such a route would serve, written to
  `models/releases/candidate_registry.json` by `adopt_candidate.py report`, so
  the unblocking is mechanical: one route that returns that file behind the same
  company wall, and one panel that renders it.
- **It did not re-run any training recipe.** Re-fitting each candidate under its
  own flags on a training split would be the clean out-of-sample comparison, and
  it is impossible here for two reasons that are both measured: three of the five
  candidates were produced by scripts other than the training script and carry
  none of its gate metadata, and two of them redefine the measurement window, so
  they do not agree with the shipped artifact on what the target is. The
  evaluation says so in words rather than averaging over the disagreement.
