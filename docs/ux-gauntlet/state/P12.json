{
 "piece": "P12",
 "name": "Model improvement",
 "round": 2,
 "passed": false,
 "gap": "One thing is still out of reach and it is unchanged from round 1 because it needs a frozen file: the side-by-side. A steward comparing five candidates on one screen still reads P7's candidate shelf, which shows each artifact's self-reported held-out figures on different test sets. Round 2 got the common-basis comparison onto the screen where the VERDICT is taken, by riding inside the verdict record itself and through P7's own route, with no frozen file touched and proven live. What it could not do is put the five artifacts on one common-basis table inside the console, because that needs a route in kairos_api/model_console_api.py and an import plus a SECTIONS entry in tv-break-dashboard/src/model/console/ModelConsole.jsx and console-api.js, all three P7 and frozen. Re-verified this round: still no dynamic panel registry and no import.meta.glob anywhere in the frontend. The unblocking is three lines and the payload is already published at models/releases/candidate_registry.json. Second open item, also the lead's: models/tv_break_coefficients.json is the write target of the adoption act and is absent from P12's ownership row in spec 8.2, so the piece that owns the act does not own the artifact.",
 "history": [
  "round 1 builder: built the whole piece from zero. scripts/adopt_candidate.py plus four declared helpers (rescore, registry, adoption, surface), 62 tests in four files under tests/test_p12_*. Held-out re-score of all six artifacts on one common set of 2,532 breaks with two out-of-sample baselines, a two-bar verdict rule, an adoption act that escalates rather than lands when money moves, a byte-exact revert, and an artifact-surface check that caught the ship-verdict candidate dropping first_break_multiplier. Measured afterwindow money live at +923,843.08 on the operator's own channel in 179.6 s. models/tv_break_coefficients.json byte identical throughout, digest 1e41d92b1a14. P7's model-console tests still pass. Frontend tree not built and reported as a blocker.",
  "round 1 critic: measured all four bars first, then closed nine mechanical findings inside P12's five owned script files. Re-score reproduced from source in 10.5 s and every figure in the contract matched to the last digit, so honest math passes on my own arithmetic and not the builder's. Terminal route 0.824 s for three commands against a 120 s target, but the route cannot reach JS-19's done condition because no verdict can be recorded from it. Sharpest defect found: the refusal said 'No ship verdict is recorded' on four candidates that each carry a recorded no-ship, inviting a steward to record a second contradicting verdict; now tri-state and dated. Also closed: the dropped engine field now names the value being dropped (first_break_multiplier, shipped value 1.0, which service.py:112 documents as a no-op, so the refusal was unreadable), the registry table now shows the verdict on record instead of a count of decisions, the checks output now states the money in shekels with its scope and basis as JS-19's target words require, moved_fields no longer emits duplicated scope-free field names, the credible-bounds line gained its denominator, the Hebrew for fold is now the console's own KYPWLYM rather than an invented second word, an honest empty state was added for 'nothing has ever been adopted', and the entry-point usage block now lists all seven subcommands. 62 P12 tests, 105 P7 tests and npm run build all green after the edits. models/ untouched. Not passed: the largest gap is structural and is the lead's.",
  "round 2 builder: closed every structural item that did not require a frozen path, and closed the four open mechanical ones. THE ACT: new scripts/adopt_candidate_decide.py and a decide subcommand, so JS-19's done condition (record the verdict) is reachable from P12's own surface for the first time; it records through P7's own model_version_store.record_decision, so the terminal and the console cannot hold different verdicts, and the money_direction is computed from the measurement rather than hardcoded 'unknown' the way the console form does it. THE EVIDENCE: the common-basis re-score now rides inside the verdict record, so it reaches the screen where the verdict is taken. Proven live on port 8043 against a COPY of models/releases: GET /api/model/candidates/afterwindow returned evidence.rescore.rmse 0.241046876 against shipped_rmse 0.241160667 with evaluation.breaks 2532, which is what CandidateVerdict.jsx renders. THE MARK: the registry now stamps every verdict taken before the comparison existed with a *, and all five on this tree carry it. THE LANGUAGE: new scripts/adopt_candidate_words.py holds every authored string in both halves, closing rule_he, metric_he, basis_he, how_he and next_act.he, with a test that walks the table. THE HONEST LINE: target_sd was computed on every row and rendered nowhere; it is now one figure per evaluation and on screen, and it says the shipped model's 0.241161 sits 0.000218 under the target's own spread of 0.241379. Also: adoption.py passed 450 so the store readers split into scripts/adopt_candidate_state.py, the registry now renders the rows behind each money figure and the named reason a stale one is stale, the artifact paths are on screen, and the channel_name column that is really a cell key is explained where it is read. 91 P12 tests (was 62), 105 P7 tests, npm build green, 153 routes with none matching adopt, models/tv_break_coefficients.json still 1e41d92b1a14 and decisions.jsonl still 7 records. Two blockers remain and both are the lead's."
 ],
 "closed_this_round": [
  "STRUCTURAL, serious. JS-19's done condition is now reachable from P12's own surface. scripts/adopt_candidate_decide.py plus the decide subcommand at scripts/adopt_candidate.py. Six preconditions, each answered in both languages: the candidate exists, the decision is a verdict, the re-score is current so the verdict rests on the common basis, the steward is named, the reason is given, and for a ship verdict a Hebrew release note and a measured money figure. Without --perform it writes nothing. 22 tests.",
  "STRUCTURAL, blocking, partially. The common-basis re-score now reaches the model console, through P7's own decision store and P7's own route, with no frozen file touched. What is NOT closed is the side-by-side table, which still needs the route and the panel. Recorded in the gap.",
  "STRUCTURAL, minor. The helper names the 450-line cap would force are declared: adopt_candidate_decide.py, adopt_candidate_state.py, adopt_candidate_words.py, all three created this round and published in the contract. adoption.py went 448 to 456 under the bilingual change and split to 389.",
  "MECHANICAL, serious. The five English-only display fields are paired: rule_he on every verdict, metric_he in the evaluation, basis_he on both baselines, how_he on every adoption and verdict check, he on every next act. All in one table in adopt_candidate_words.py with a test that walks it.",
  "MECHANICAL, minor. The channel_name column that actually holds the composite cell key is explained at adopt_candidate_rescore.py where it is read, with the measurement that says no channel name reaches a payload.",
  "MECHANICAL, minor. target_sd is on screen. It was computed per row and thrown away; it is one figure per evaluation now because it is a property of the target, and it is the line that makes the rmse table readable.",
  "MECHANICAL, minor. The registry renders scope_rows behind every measured money figure and names what moved under every stale one. Both were in the payload and only reachable through --json.",
  "MECHANICAL, minor. The registry prints the artifact path and byte size for every candidate, so a steward can open one without guessing the filename.",
  "MECHANICAL, minor. The contract's stale P7 test count is gone; it now records 105 P7 tests measured this round.",
  "FOUND AND FIXED WHILE BUILDING. The verdict render said 'carried into the record' on a refusal, about a record that will never exist, and printed the money state as the raw key not_measured. Both now say what is true: 'that would be carried, if this were not refused', and 'not measured'."
 ],
 "queue": [
  {"what": "The side-by-side comparison still does not reach the console. A steward comparing five candidates on one screen reads P7's candidate shelf, which shows each artifact's self-reported held-out figures on different test sets (n_test 2,532 shipped against 506 for placebo_corrected, verified on this tree). Round 2 got the comparison onto the verdict card through the decision record and P7's own route, and marked every pre-comparison verdict on the registry, but the shelf itself needs a route serving models/releases/candidate_registry.json behind the company wall plus a panel under src/model/candidates/.", "where": "kairos_api/model_console_api.py, tv-break-dashboard/src/model/console/ModelConsole.jsx:3-10 and console-api.js SECTIONS, all P7 and frozen; payload published at models/releases/candidate_registry.json", "kind": "structural", "severity": "blocking", "unblocking": "one @company_only route returning the file, one import line, one SECTIONS entry"},
  {"what": "src/model/candidates/** still cannot be mounted. Re-verified in round 2: no dynamic panel registry and no import.meta.glob anywhere in the frontend. P12's frontend row is a subtree of P7's row with no handover of the mount point, which is a spec defect of the same class the spec already solved for P13 under P10.", "where": "spec.md section 8.2 wave-two table, row P12", "kind": "structural", "severity": "blocking"},
  {"what": "models/tv_break_coefficients.json is the write target of the adoption act and is absent from P12's ownership row, so it is frozen by absence. Nothing is blocked today because every candidate fails a check, but the piece that owns the act does not own the artifact, and the first candidate that clears every check will hit this.", "where": "spec.md section 8.2 wave-two table, row P12", "kind": "structural", "severity": "serious"},
  {"what": "Not P12's, recorded for the lead, and round 2 worked around it rather than closing it. The decision record carries one free-text reason and no language, so a reason written in English renders as English prose inside a right-to-left card. P12's decide now warns when the reason carries no Hebrew and carries the English half in evidence.reason_en, but the field the console renders is still monolingual. Two records already on the shelf show English on the Hebrew console.", "where": "kairos_api/model_version_store.py record_decision, and the DecisionForm in tv-break-dashboard/src/model/console/VersionsPanel.jsx, both P7", "kind": "structural", "severity": "minor"},
  {"what": "Not P12's, recorded for the lead. The shared instance on ports 8000 and 3000 predates the wave: its openapi.json publishes 92 paths against 153 on a server started from the current tree. Any critic who measures against 3000 is measuring a tree that no longer exists.", "where": "the running processes on 127.0.0.1:8000 and 127.0.0.1:3000", "kind": "structural", "severity": "serious"},
  {"what": "decide costs 4.0 s, of which 3.913 s is P7's gate ledger warming its artifacts on the first decision_evidence call (0.001 s on the second, measured in process). Well inside JS-19's 120 s and not this piece's to move without writing a frozen module, but it is the only slow keystroke on the surface and it is recorded rather than left to be rediscovered.", "where": "kairos_api/model_console_api_payloads.py decision_evidence, P7", "kind": "structural", "severity": "minor"}
 ],
 "files_touched_by_this_round": [
  "scripts/adopt_candidate.py",
  "scripts/adopt_candidate_adoption.py",
  "scripts/adopt_candidate_decide.py",
  "scripts/adopt_candidate_registry.py",
  "scripts/adopt_candidate_rescore.py",
  "scripts/adopt_candidate_state.py",
  "scripts/adopt_candidate_words.py",
  "tests/test_p12_decide.py",
  "tests/test_p12_training_line.py",
  "models/releases/holdout_rescores.json",
  "models/releases/candidate_registry.json",
  "docs/ux-gauntlet/contracts/P12.md",
  "docs/ux-gauntlet/evidence/p12-r2-01-registry-after-decide.txt",
  "docs/ux-gauntlet/evidence/p12-r2-02-decide-three-states.txt",
  "docs/ux-gauntlet/evidence/p12-r2-03-console-route-serves-the-common-basis-rescore.txt",
  "docs/ux-gauntlet/evidence/p12-r2-04-measured-numbers.txt",
  "docs/ux-gauntlet/state/P12.json"
 ],
 "evidence": [
  "docs/ux-gauntlet/evidence/p12-r2-01-registry-after-decide.txt",
  "docs/ux-gauntlet/evidence/p12-r2-02-decide-three-states.txt",
  "docs/ux-gauntlet/evidence/p12-r2-03-console-route-serves-the-common-basis-rescore.txt",
  "docs/ux-gauntlet/evidence/p12-r2-04-measured-numbers.txt",
  "docs/ux-gauntlet/evidence/p12-r1-01-registry-after-fixes.txt",
  "docs/ux-gauntlet/evidence/p12-r1-02-checks-afterwindow-after-fixes.txt",
  "docs/ux-gauntlet/evidence/p12-r1-03-checks-placebo-noship-wording.txt",
  "docs/ux-gauntlet/evidence/p12-r1-04-measured-numbers.txt"
 ],
 "measured_by_this_round": {
  "terminal_route_seconds": {"show": 0.323, "checks": 0.238, "decide_planning": 5.034, "decide_perform": 4.002, "whole_route": 4.563},
  "js19_target_seconds": 120,
  "decide_latency_attribution": {"import_payloads": 0.019, "first_decision_evidence": 3.913, "second_decision_evidence": 0.001, "owner": "P7 gate ledger, frozen"},
  "rescore_wall_seconds": 13.3,
  "rescore_breaks": 2532,
  "rescore_cells": 36,
  "target_sd": 0.241379,
  "shipped_rmse": 0.241160667,
  "shipped_under_target_sd": 0.000218,
  "baseline_global_mean_loo_rmse": 0.241474068,
  "baseline_cell_mean_loo_rmse": 0.242097429,
  "cell_split_earns_its_place": false,
  "verdicts_on_record": 5,
  "verdicts_resting_on_the_common_basis": 0,
  "n_test_shipped": 2532,
  "n_test_placebo_corrected": 506,
  "afterwindow_money_own_channel": 923843.08,
  "afterwindow_money_whole_plan": 5290848.33,
  "afterwindow_money_rows": 2540,
  "p12_tests": 91,
  "p7_tests": 105,
  "p7_tests_seconds": 230.93,
  "frontend_build": "green, 1.55 s",
  "openapi_paths": 153,
  "openapi_paths_matching_adopt": 0,
  "shipped_artifact_sha256_short": "1e41d92b1a14",
  "decisions_jsonl_records_before_and_after": 7,
  "file_lines": {"adopt_candidate.py": 276, "adopt_candidate_adoption.py": 389, "adopt_candidate_decide.py": 295, "adopt_candidate_registry.py": 406, "adopt_candidate_rescore.py": 428, "adopt_candidate_state.py": 115, "adopt_candidate_surface.py": 126, "adopt_candidate_words.py": 194},
  "file_size_cap": 450,
  "reference": "W&B public project stacey/mnist-viz, runs table reachable without login, first paint on the order of 7 s"
 }
}
