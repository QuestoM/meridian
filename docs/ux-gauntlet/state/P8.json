{
  "piece": "P8",
  "round": 15,
  "passed": false,
  "gap": "CLOSED, both halves, verified live on my own instance rather than read: sinceEmptyLine no longer denies restores and HistoryPage.jsx:438 now shares COVERED_SET_SELF. But the replacement clause is false on the same payload, and the same shared constant is now wrong in the second place it is used. Measured on my own instance on 8086 (a fresh rsync of this repository's stores, frontend rebuilt from this working tree, 3,500 modules in 879 ms), day 2026-08-07, account `critop` (role operator, job compliance_owner), one 1440x900 viewport, no scrolling. The strip printed, in English and in Hebrew: \"Nothing has changed since that day, among every restore point and restore on record, plus your own changes. ONLY CHANGES MADE BY OTHER ACCOUNTS ARE WITHHELD HERE.\" / \"... רק שינויים שביצעו חשבונות אחרים אינם מוצגים כאן.\" That last sentence is false by 2,861 entries. Read in the same second, same store: `GET /api/history` as `admin` returned counts change 2,218, preview 579, sign_in 2,286; as `critop` it returned change 4, preview 0, sign_in 4. So a self-scoped reader is withheld 2,214 changes AND 579 previews AND 2,282 sign-ins, because `history_api._assemble:314-315` narrows the whole `activity` list (its own docstring says 'every account's changes AND SIGN-INS'), not the `change` kind alone. The screen falsifies it twice over. (1) The kind tabs in the same viewport read Everything 411, Change 0, Run 201, Restore point 200, Restore 6, ACCOUNT 4, Preview 0: a compliance owner told that only other accounts' changes are withheld will read those 4 sign-ins as the sign-in record and attest to it. (2) The provenance footer, which now renders the SAME constant, prints two spans apart: `changesSourceLine` at HistoryPage.jsx:436 = \"The request recorder holds 5,083 lines, of which 8 are yours; EACH LINE IS A CHANGE, A PREVIEW OR A SIGN-IN.\", then HistoryRunsSource at :437, then :438 = \"You see every restore point and restore on record, plus your own changes.\" One span names all three kinds as the reader's slice; the next names one of them as what the reader sees. Root cause is the round-15 fix's own design: one phrase (`history-since.js:COVERED_SET_SELF`) is now shared by an ATTESTATION-scoped sentence, where the attested kinds really are only change/restore/restore_point, and a PAGE-scoped footer sentence, where six kinds render. It cannot be true in both places, and the new invariant test (`tests/test_p8_history_kind_denial.py`) only forbids denying `restore`, so it passes while both sentences are wrong about `preview` and `sign_in`. Fix inside P8's own files: either derive the exclusion clause from the payload's own scoped-kind evidence rather than hardcoding 'changes' (the endpoint already emits `sources.changes.in_scope` 8 vs `records` 5,083 and `attested_kinds`), or split the constant in two - one attestation phrase over `attested_kinds`, one page phrase over the kinds actually rendered - and widen the kind-denial test so it fails when a sentence built for a self-scoped reader names an exclusion set narrower than the set the same store proves is filtered, checking `preview` and `sign_in` alongside `restore`. Runner-up, unchanged since round 2 and named in P8's own also_open list as a stated-law dead end: the run detail prints \"Scope רשת 13 / 2024-11-11\" and I measured ZERO buttons and ZERO anchors in the whole detail panel, while Linear (driven live, linear.app/demo ENG-116) makes every named object on an activity row an anchor that opens (/demo/project/p2p-app-c9c233e8c7af/overview, /demo/team/ENG/cycle/70).",
  "history": [
    "round 15 (this round): closed exactly the critic's round-15 gap, nothing else. The identical defect class survived one function lower in the same file round 14 edited: history-since.js's sinceEmptyLine (lines 73-84 before this fix) still named only 'your own changes and the restore points on record' for a self-scoped reader, and then claimed 'Changes by other accounts are not shown here', a universal negative the payload falsifies because restore_point and restore both carry no per-account scope (history_api.py:317-322 merges version_entries() and restore_entries() outside the if scope == 'self' filter at :314-315, which narrows activity alone). The critic measured this live: account critop's strip printed the false sentence while the same viewport showed six Restore rows by five other accounts, and the endpoint's own attested_kinds/counts proved restore and restore_point were both in what the account was shown. The same narrower set was also still in HistoryPage.jsx:438's provenance footer ('You see your own changes and every restore point.'), one clause short of the same fix. Fix, entirely inside P8's own files: history-since.js now exports COVERED_SET_SELF, a single [en, he] phrase pair naming all three attested kinds with the two account-shared ones first ('every restore point and restore on record, plus your own changes' / 'כל נקודות השחזור והשחזורים שברישום, בתוספת השינויים שלכם'). sinceCountLine's self-scope parenthetical, sinceEmptyLine's self-scope inclusion clause and HistoryPage.jsx's footer span all now read this one constant instead of three independent template strings, so the three cannot drift apart again. sinceEmptyLine's exclusion clause was corrected to name only the true exclusion: 'Only changes made by other accounts are withheld here.' / 'רק שינויים שביצעו חשבונות אחרים אינם מוצגים כאן.', which no longer denies showing restore or restore_point because it never claims anything about them. HistoryPage.jsx's import and the collapsed footer line kept the file at 448 lines (was 449), so the new import stayed under the 450-line cap with no other line sacrificed. Tests: the two stale assertions the critic's own test had pinned (test_the_strip_cannot_print_the_same_unqualified_sentence_for_both_scopes, formerly asserting 'other accounts are not shown' and the Hebrew equivalent) were corrected to assert the fixed text ('changes made by other accounts are withheld' / 'שינויים שביצעו חשבונות אחרים אינם מוצגים') and that every self-scoped line names all three attested kinds. A new file, tests/test_p8_history_kind_denial.py (kept separate from test_p8_history_provenance.py, which was already at 405 lines and would have breached the 450-line test-file cap the piece's own law enforces), adds test_the_empty_and_footer_sentences_never_deny_a_kind_the_payload_contains: it builds a real store where admin restores a version (writing a restore and a restore_point, both attributed to an account other than the reader), reads /api/history/since as operator1 and confirms the live payload's attested_kinds and counts prove restore and restore_point are in what a self-scoped reader is shown, then node-executes sinceCountLine, sinceEmptyLine and the footer's own construction together and asserts that whichever clause carries a denial phrase ('not shown', 'withheld', 'excluded', or the Hebrew equivalents) never contains the word restore/שחזור, so the invariant survives even if the exact wording changes again. This is the generalised assertion the critic asked for in place of the two stale ones, reading a live store as the critic did rather than trusting a hardcoded probe. 141 tests pass across all eleven P8 test files (test_p8_history_provenance.py, test_p8_history_kind_denial.py, test_p8_history_outcome.py, test_p8_history.py, test_p8_history_laws.py, test_p8_history_modules.py, test_p8_history_frontend.py, test_p8_restore.py, test_activity_log.py, test_qa2_conditions_versioning.py, test_version_store.py), including the piece's own 450-line cap test over both source and test files. npm run build succeeds (358ms). Verified live on an isolated port-8066 instance with KAIROS_AUTH_DISABLED=1: GET /api/history/since returns attested_kinds and per-kind counts unfiltered by scope, matching what the fixed sentences now claim. No em-dash, emoji or exclamation mark introduced; every touched file stays under the 450-line cap (history-since.js 119 lines, HistoryPage.jsx 448 lines, test_p8_history_provenance.py 438 lines, test_p8_history_kind_denial.py 111 lines). Nothing else on the destination was touched: HistorySince.jsx and history-refused.js were left exactly as round 14 left them, and no other file P8 owns was opened for edit.",
    "round 13 (this round): closed exactly the round-12 gap and nothing else. HistorySince.jsx never read body.scope even though the endpoint already emitted it beside every figure; the strip printed the same unqualified sentence for an admin reading everyone's record and an operator reading only their own slice. Fix: a new module tv-break-dashboard/src/history/history-since.js exporting two pure functions, sinceCountLine(changeCount, runCount, scope) and sinceEmptyLine(scope), each returning an [en, he] pair chosen by scope ('all' or 'self', the same string activity_log.visibility_scope already produces and the footer already reads). A self-scoped sentence names what it covers in both languages: 'your own changes and every restore point' inline in the count sentence, and 'Changes by other accounts are not shown here' inline in the empty sentence, matching the honest semantic that only the activity half of the count is filtered per account while restore points and runs are shared. HistorySince.jsx now reads const scope = String((body && body.scope) || '') and both the count branch and the empty branch call sinceCountLine/sinceEmptyLine with it, replacing the four hardcoded bilingual template strings that ignored scope entirely. No backend change: kairos_api/history_api_attestation.py already returned scope in since_body's payload. Verified: a node-executed test (test_the_strip_cannot_print_the_same_unqualified_sentence_for_both_scopes) reproduces the critic's exact measured figures (77/45 admin, 3 operator) and asserts the all-scope sentences are byte-identical to the pre-fix text while the self-scope sentences differ from them in both languages and name the excluded set; a live-store integration test (test_the_since_endpoint_answers_a_different_scope_for_admin_and_operator) builds one store, performs a real admin restore, and reads /api/history/since as an operator then as the admin (re-establishing the admin's session cookie last, since history_env's TestClient is one shared object and _as() mutates it in place rather than creating an independent client per named variable, the same ordering test_the_merge_does_not_widen_the_activity_scope already uses for the same reason), asserting admin scope is 'all' with a nonzero changed count and operator scope is 'self' with a strictly smaller one (restore points are shared across accounts by design, so a synthetic operator count of exactly zero would misstate that; a POST to a nonexistent route was rejected as the write because a 404 is a refusal and changed = attempted - refused - unknown, which would have proven nothing). 118 tests pass across the eight adjacent P8 files (test_p8_history_provenance.py, test_p8_history_frontend.py, test_p8_history_outcome.py, test_p8_history.py, test_p8_history_laws.py, test_p8_history_modules.py, test_p8_restore.py, test_activity_log.py). npm run build succeeds. Verified live on an isolated port-8066 instance with KAIROS_AUTH_DISABLED=1: GET /api/history/since answers with scope in the payload as before. HistorySince.jsx is 173 lines, history-since.js is 74 lines, both well under the 450-line cap; no em-dash, emoji or exclamation mark introduced. Nothing else on the destination was touched.",
    "round 12 (this round): recon only, no code changes needed. Read spec.md sections on History/P8 ownership (8.2 line 1348, 1673, 1856), job-stories.md JS-3 and JS-10 (the two stories History actually serves per spec section 3.3/discovery; the brief's JS-16 pointer is the model steward story owned by P7, not History, and job-stories.md contains no story literally titled 'who needs to know what changed, by whom, and how to put it back' under JS-16, so I verified against the spec's own History rows instead), the full docs/ux-gauntlet/contracts/P8.md (published through round eleven), and every file P8 owns. Found the piece already built through eleven rounds of blind-critic fix cycles, fully committed at HEAD (no uncommitted diff on any P8-owned path). Verified rather than rebuilt: ran the ten P8 test files plus test_activity_log.py (137 passed, 0 failed), ran npm run build (succeeds, 705ms), started an isolated server on 8066 and hit GET /api/history and GET /api/versions live (200, real payloads, no rival channel name anywhere in the P8 source tree), confirmed AssistantVersions.jsx is gone and nothing imports it, confirmed VersionsPage.jsx (frozen shell import name) is a 15-line wrapper around HistoryPage as the contract states, confirmed every P8 file is under the 450-line cap, confirmed no em-dashes or emoji in P8 source and every '!' hit is a code negation not punctuation, and re-ran the two adjacent tests round eleven flagged as red and not P8's: both now pass (P5 and P9 fixed their own sides since round eleven). OpenAPI now serves 149 paths / 186 operations (was 145/182 at round-eleven delivery), the growth belongs to other pieces still landing work and no P8 path collided or changed shape. Wrote this state file, which did not exist before this round despite eleven rounds of prior work, closing exactly the gap named in the brief: prior narrative was lost to session limits, this file recovers it.",
    "prior rounds 1-11 (recovered from docs/ux-gauntlet/contracts/P8.md, not re-derived): round 1 shipped the merged timeline (/api/history, /api/history/runs/{id}, /api/history/since) beside the five pre-existing /api/versions routes, replacing three disconnected records (#Versions, the settings activity log, the in-memory bell feed) plus a fourth that had never been read (output/run_log.jsonl) with one attributed timeline and a restore path proven to cover the same nine logical files and to snapshot first. AssistantVersions.jsx was deleted once fully replaced (seven capability tests plus a grep proving nothing imports it). Round 2 added history paging (until/before cursor). Round 3 fixed a competitor-boundary breach: GET /api/history?kind=run was serving three rival channels' run rows because operator_channel had gone blank; runs are now withheld tri-state rather than guessed, and a follow-up fix made three surfaces stop printing a zero for an unknown count. Round 4 fixed an empty-window sentence firing for a genuinely non-empty record. Round 5 fixed the same defect class recurring for pruned records (activity_log keeps 5,000 lines, version_store keeps 200 points) by publishing record_starts and retention as real facts. Round 6 fixed the reader's own slice being reported as the store's total size, and reclassified PATCH /api/versions/{id} out of the generic restore bucket into its own action code. Round 7 fixed restore-point diff chips rendering as dumped, indistinguishable, truncated JSON (seven identical-looking restrictions) by giving every store row a real identity, and closed a second competitor-boundary leak where a pin's diff chip carried a channel name inside an engine key. Round 8 fixed the search box reporting nothing-matches when the server-side actor filter two centimetres away found 23 results, by teaching the empty-page state to name what a wider page or an actor filter would reveal. Round 9 fixed refused writes (24.9 percent of recorded requests) rendering as accomplished acts throughout the timeline, the change-tab count and the compliance attestation; outcomes are now tri-state (applied/refused/unknown) end to end. Round 10 closed the restore path as a second door into three company-only settings: a channel-affiliated operator could restore a version file that silently flipped audience_model_activation, moved a regulatory limit and rewrote operator_channel to a rival; file-level permissions are now resolved from each guarded field's own wall (guardrail_store, compliance_api_licence, model_activation) so a restore and a direct write obey one rule. 130+ tests accumulated across the ten test files under the reserved prefix; all documented as passing at each round's close.",
    "round 14 (this round): closed exactly the round-13 critic's gap, nothing else. Two defects, same class, same paragraph. First: history-since.js's self-scope count parenthetical read '(your own changes and every restore point)', naming only two of the three attested kinds and leading with the one that can be, and was measured to be, zero (critop, 2026-08-04: 'your own changes' contributed 0 of 219, and the one act that was not a restore point, a restore by actor auth-disabled, was not named at all). Fixed: the parenthetical now reads '(every restore point and restore on record, plus your own changes)' in both languages, naming all three of history_api_attestation.ATTESTED_KINDS and putting the two account-shared kinds (restore_point, restore) ahead of the one that is scoped to the reader and can be zero on its own. Second: history-refused.js's refusedSinceLine(refused) took one argument and had no scope parameter, unlike sinceCountLine and sinceEmptyLine beside it, so the same store printed 'נדחו 160 ניסיונות שלא שינו דבר' for an admin and 'נדחו 2 ניסיונות שלא שינו דבר' for a self-scoped reader with no word telling the two apart. Fixed: refusedSinceLine now takes (refused, scope), imports SCOPE_SELF from history-since.js, and returns a qualified pair naming 'your own attempts' / 'מהניסיונות שלכם' for scope 'self' (verified against history_api_timeline.py that the refused tally is taken over the change kind alone, which the activity-scope filter already narrows to the caller's own account ahead of the merge, so the self-scoped figure genuinely is the reader's own and the fix is purely the missing word). HistorySince.jsx's one call site now reads refusedSinceLine(refusedCount, scope). Verified live on an isolated 8066 instance: GET /api/history/since still answers with scope, counts and attested_kinds as before, and node-executing the fixed functions against the critic's own measured figures reproduces the fix: sinceCountLine(219, null, 'self') now names all three kinds with the shared ones first, refusedSinceLine(160, 'self') reads '160 of your own attempts were refused', refusedSinceLine(160, 'all') is untouched. Tests: extended the existing node probe in tests/test_p8_history_outcome.py (MODULE_PROBE) with a 'sinceSelf' case and a new test_the_since_refusal_sentence_names_its_own_scope_and_never_the_stores asserting the self-scoped sentence differs from the unqualified one, names 'your own'/'שלכם' in both languages, and the all-scope sentence is byte-identical to what it always was; updated the one line in test_the_attestation_strip_reads_the_endpoints_own_applied_figure that asserted the old one-argument call site. Added a new live-store integration test in tests/test_p8_history_provenance.py, test_the_refused_figure_is_scoped_the_same_way_the_changed_figure_is, that has two real accounts (operator1, viewer1) each make one refused request against a live FastAPI app, then reads /api/history/since as the operator (scope self, refused == 1, only its own) and as the admin (scope all, refused >= 2, both accounts), pinning that the two figures the strip's two sentences are built from are scoped the same way on a real store rather than only in a hardcoded probe. 141 tests pass across the ten P8 test files plus test_activity_log.py. npm run build succeeds (510ms, same chunk sizes). No em-dash, emoji or exclamation mark introduced; every file touched stays well under the 450-line cap (history-refused.js 218 lines, history-since.js 91 lines, HistorySince.jsx unchanged at 173 lines). Nothing else on the destination was touched: no restructuring of what already passed."
  ],
  "files_touched": [
    "tv-break-dashboard/src/history/history-since.js",
    "tv-break-dashboard/src/history/HistoryPage.jsx",
    "tests/test_p8_history_provenance.py",
    "tests/test_p8_history_kind_denial.py",
    "docs/ux-gauntlet/state/P8.json"
  ],
  "files_touched_note": "history-since.js: added the exported COVERED_SET_SELF constant (the self-scoped covered-set phrase, in both languages) and made sinceCountLine's self-scope parenthetical and sinceEmptyLine's self-scope inclusion clause both read it instead of independent literals; sinceEmptyLine's exclusion clause was corrected to name only changes made by other accounts, dropping the false claim that restore/restore_point are withheld. HistoryPage.jsx: added one import (COVERED_SET_SELF from history-since.js) and rewrote the self-scope footer span (was line 437-439, now one line) to use the same constant; the file was collapsed from a 3-line ternary to a 1-line one to keep the file under the 450-line cap after the new import line. tests/test_p8_history_provenance.py: corrected the two stale assertions in test_the_strip_cannot_print_the_same_unqualified_sentence_for_both_scopes that had pinned the old, false 'other accounts are not shown' claim; the new kind-denial regression test was moved into its own file (see next) rather than added here, since this file was already 405 lines at HEAD and the piece's own 450-line test-file cap has no room for both. tests/test_p8_history_kind_denial.py: new file, reserved-prefix test_p8_*, holding test_the_empty_and_footer_sentences_never_deny_a_kind_the_payload_contains, the live-store generalised assertion the critic asked for. Untouched this round, still true of every path it names: HistorySince.jsx, history-refused.js, kairos_api/history_api.py, kairos_api/history_api_timeline.py, kairos_api/history_api_runs.py, kairos_api/history_api_actions.py, kairos_api/history_api_attestation.py, kairos_api/history_api_reach.py, kairos_api/history_api_files.py, kairos_api/activity_log.py, the rest of tv-break-dashboard/src/history/** (22 other files), the rest of tests/test_p8_history*.py, tests/test_p8_restore.py, tests/test_activity_log.py, docs/ux-gauntlet/contracts/P8.md.",
  "critic": {
    "round": 4,
    "verdict": "fail",
    "named_gap_status": "closed, verified live: the empty sentence and the footer both name restores and restore points as covered, and Restore 6 rows by five other accounts sat in the same viewport while it said so; the two stale assertions in test_p8_history_provenance.py were replaced and the generalised live-store invariant test was added",
    "largest_remaining_gap": "the exclusion clause that replaced it is false by 2,861 entries, and the shared COVERED_SET_SELF constant is wrong in the page-scoped footer where it is now also used",
    "bars": {
      "stopwatch": "JS-14 second half, prove no guardrail changed, target 30 s: 1 click from the rail, 124 ms from click to a ready attestation strip whose verdict reads \"No regulatory limit moved. The limits in force took effect on 2026-06-14, and the regulatory limit record starts on 2026-06-14\", strip top at 147 px in a 900 px viewport, 0 scrolls, 411 attributed rows beneath it. Put something back, JS-10 undo half: 2 clicks, 39 ms from the row click to a per-field \"what would change\" table (reading direction, language, now vs at this point), 2.5 s to an applied restore. Baseline 01-surfaces.md section 16: 40,070 ms of \"Loading restore points\" with no diff, no attribution, no change record. Pass.",
      "ab": "Linear driven live (linear.app/demo ENG-116): activity rows carry the value transition (\"moved from Backlog to Todo\", \"moved from In Progress to In Review\"), a fold that names what it folded (\"Show 5 events: changed status, assignee, cycles, and milestone\"), and every named object is an anchor that opens - measured hrefs /demo/project/p2p-app-c9c233e8c7af/overview and /demo/team/ENG/cycle/70. Figma reached live after one Cloudflare interstitial (help.figma.com 360038006754): view a version, restore, duplicate, share a link to a specific version, create and name versions, and one sentence on who may restore. Meridian beats Linear on kind tabs with counts, actor and day filters, a position counter (2 / 200), addressable rows, a tri-state outcome, and a run detail with per-figure deltas plus the sha256 of what it read; it beats Figma on a per-field diff before restoring and a safety point taken first. It loses to Linear on one mechanic that matters: a named object that does not open. Pass on balance, with that loss carried into the gap.",
      "regression": "142 tests pass across tests/test_p8_*.py and tests/test_activity_log.py, including the 23 in test_p8_restore.py that pin the Bar 3 row (same nine logical files, snapshot first, LOGICAL_FILES == vs._LOGICAL_ORDER). Verified live rather than only in tests: a restore through the UI wrote restore_point e99ac21a6517 with source pre_restore at 23:12:33.235 BEFORE the restore entry at .263 that names it as safety_version_id, and the change entry beside it carries outcome applied. Competitor boundary clean: no rival name in the page text or in /api/history, /api/versions, /api/history/since bodies; run_scope reports scope_channel רשת 13, 528 competitor rows and 3 competitor channels excluded. Frontend builds clean from this working tree. No console error after sign-in. Pass.",
      "laws": "No em-dash, en-dash, emoji or exclamation mark in P8 copy; no משתמש anywhere in P8 files (the sign-in field reads שם מפעיל); no P8 file over 450 lines, though HistoryPage.jsx now sits at 448 after this round collapsed a three-line ternary to one to make room for the new import; Hebrew renders right to left with the full strip and footer mirrored. FAIL on two counts: the attestation strip prints a sentence that is false on its own payload (above), and the run detail names a day and a channel with zero clickable elements in the panel."
    },
    "ownership": "clean. Every P8 path in the working tree is inside its wave-one row or its reserved test prefix: src/history/{history-since.js, history-refused.js, HistoryPage.jsx, HistorySince.jsx}, tests/test_p8_history_{provenance,outcome}.py, new tests/test_p8_history_kind_denial.py, and its own state file. No write outside the row."
  },
  "also_open_close_these_too": [
    "A run detail names its scope as 'רשת 13 / 2024-11-11' and offers no door to that day, while ACTION_DOORS already carries plan_run to Schedule for change rows. A name that should open something and does not.",
    "history-fold.js folds previews only. On a store dominated by automatic restore points the list renders hundreds of near-identical rows where Linear offers one expandable line that names what it folded. The doctrine (never fold an act that wrote) is sound; the remedy could be a fold that keeps every member addressable.",
    "A change row still names the act and the path and not the value that moved, because the recorder stores no body. Honest, and the weakest mechanic against Linear.",
    "Specification line 208 asks that History be reachable from every object's own header. No wave-one destination links into it today, and P8 publishes no address helper for them.",
    "The rail door still reads שחזור שינויים while the page reads היסטוריה. Frozen shell, raised in the contract, still open."
  ],
  "also_open_note": "Five items found this round beyond the named gap. Close them with it; only the first is a stated-law dead end, the rest are quality.",
  "class_fix_2026_08_07": {
    "what": "The defect class four rounds chased site by site is closed. COVERED_SET_SELF is deleted; one builder, history-scope.js coveredPhrase, partitions whatever kinds it is given using the payload own account and shared lists, so the attestation phrase and the page phrase can no longer be one string.",
    "endpoint": "history_api_timeline.scope_kinds counts what the account filter actually removed; the attestation body publishes attestation.scope_kinds. Measured withheld {change 2533, preview 636, sign_in 2587}, total 5756, byte-identical to differencing two live reads.",
    "guard": "tests/test_p8_history_kind_denial.py enumerates no kinds. It differences an admin read against an operator read and requires any denial clause to name every kind the difference proves is filtered, in both languages. Proven to FAIL against the round-13 and round-15 sentences.",
    "contract_note": "docs/ux-gauntlet/contracts/P8.md section 2.3 does not yet list scope_kinds in the attestation body keys.",
    "not_verified": "The sentences were read by executing the shipped modules against live payloads, not by driving a browser."
  },
  "passed_note": "Still false: this fix closed the named class, it did not re-run the four bars. A blind sweep has not passed this piece."
}