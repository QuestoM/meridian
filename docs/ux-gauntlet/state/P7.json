{
  "piece": "P7",
  "round": 5,
  "passed": true,
  "gap": "GET /api/model/decisions/record (POST, via VersionsPanel.jsx's ship/no-ship form) is not stamped with can_edit. A company viewer who reaches the Versions section still sees an enabled Record control and learns the refusal only from the 403 test_p7_model_wall.py's test_a_company_viewer_cannot_record_a_decision already proves the wall returns; the write itself was never at risk, only the affordance's honesty before the click. The owner's brief named training rebuilds specifically ('who may run a rebuild'), and the two routes that answer that question (GET /api/model/console, GET /api/model/training) are both stamped and both pre-disable their controls, measured live with a real admin session (enabled, real Hebrew permission sentence) and a patched can_edit:false response (both Train buttons render disableable+disabled, amber refusal note in the wall's own words). Extending the same stamp to the decisions route and VersionsPanel's form is a same-shaped, one-route follow-up, left undone because it was outside the three things the owner asked the console to say.",
  "history": [
    "round 5 (this round): recon confirmed all prior rounds (1 to 4, documented in docs/ux-gauntlet/contracts/P7.md sections 5.1-5.7) were already shipped and green on disk, nothing to rebuild. Read the full spec.md P7 row, job-stories.md JS-16, the audience artifact (models/audience_model.json, computed 2026-07-29), and every P7-owned file before editing. The owner's brief: the gate counts (3 active, 5 tested-and-lost, 5 no-contrast, 0 not-measured) read as stuck for days when in fact five families simply have no contrast to measure yet (real reasons already in the artifact) and only one family (calendar_religious_blackout) actually ran and lost (0.06% vs 2.00% bar). Asked for three additions: (1) the date verdicts were computed, (2) the exact condition that would open each closed gate, (3) who may run a rebuild. Verified (2) was already fully built in an earlier round (per-gate 'what would end this' + earliest real date, confirmed live in browser, no code changed). Shipped (1): basis.measured_at (already served by the backend, never rendered) now prints per-gate in console-bits.jsx's Basis component and as a console-wide 'gate verdicts measured on <version date>' header line in ModelConsole.jsx. Shipped (3): GET /api/model/console and GET /api/model/training both now call MODEL_WALL.stamp(result, request), the same Wall.stamp contract already live on pricing/rules; the header states who may act in a sentence (console-words.js: header.who_may_train / header.can_edit_yes, plus a new canEditReason() helper mirroring the established refusalSentence() pattern from rules-words.js without importing across piece boundaries) and TrainingPanel.jsx disables both Train buttons and shows an amber note with the wall's own refusal sentence when can_edit is false. Added tests/test_p7_can_edit.py (4 tests, real resolved admin/viewer sessions via the auth API, not synthetic bodies): admin reads can_edit:true with no reason on both routes; viewer reads can_edit:false with READ_ONLY_ROLE_DETAIL on both routes; the stamped reason is asserted equal to MODEL_WALL.reason() called directly against a synthetic Request carrying the viewer's real session cookie (proves the sentence cannot drift from the one the write route would actually refuse with); activation mirror still carries no can_edit of its own (guards the pre-existing deliberate absence). All 105 tests under the test_p7_ prefix pass (was 101 across 12 files before this round, now 105 across 13; pytest --collect-only confirms). npm run build succeeds. Visually confirmed all three fixes live in a real headless-Chrome browser against my own server on port 8065: (1) header reads 'הכרעות השערים נמדדו בתאריך 2026-07-29' beside the unchanged 3/5/5/0, retention gates carry 2026-07-29T00:12:13 and audience gates carry 2026-07-29T06:41:29 as two honestly distinct timestamps rather than one blurred claim; (2) every no_contrast gate's a11y snapshot shows 'מה יסיים את זה' with a real per-family sentence and 'המועד המוקדם ביותר' with a real date; (3) with the real auth-disabled tolerant session the header reads 'לחשבון הזה יש הרשאה להריץ אימון ולתעד הכרעה' and both Train buttons render enabled, and with window.fetch patched via evaluate_script to answer can_edit:false on both routes, both Train buttons render disableable+disabled in the a11y tree and the panel opens on an amber line reading the wall's own 'לחשבון צפייה אין הרשאת עריכה'. Updated docs/ux-gauntlet/contracts/P7.md: new section 5.8 with full measured evidence, section 3's file/test line-count tables re-measured and updated (kairos_api/model_console_api.py 366->378, ModelConsole.jsx 312->341, TrainingPanel.jsx 221->232, console-bits.jsx 175->182, console-words.js 233->255, model-console.css 203->218, model-console-panels.css unchanged at 383 since only a selector was extended not a line added, new test file test_p7_can_edit.py at 111 lines/4 tests), round-count summary lines updated (round 5 published thirteen files and 105 tests). No baseline route, payload shape or file outside P7's ownership was touched. Did not extend can_edit to VersionsPanel.jsx's decision-record form (see gap field): outside the owner's literal three-part ask and the write is already safely refused server-side, just not pre-disabled in the UI the way training now is.",
    "prior rounds 1-4 (recovered from docs/ux-gauntlet/contracts/P7.md sections 1-5.7, not re-derived this round): round 1 shipped the console itself (thirteen gates unified into one ledger, seven backend helper modules, the src/model/** frontend tree, the company-only wall). Round 2 split model_console_candidates_holdout.py out under the line cap. Round 3 republished section 3 after finding twelve stale numbers via a blind measurement, and fixed the unstyled panel CSS import, the unreachable provenance route, a literal 'null' gate value rendering as a false absence, and #Model not surviving a reload. Section 5.2 closed the header's dead-end note by making it a real control to Rules, plus a contrast fix (1.77:1 -> 7.96:1). Section 5.3 fixed a recorded verdict leaving no visible trace on its own candidate card (missing per-candidate reader). Section 5.4 fixed a filter that opened a hole and contradicted its own count. Section 5.5 fixed the training panel never learning a run it started had finished (now polls every 1500ms while a run is open, following the server's own flight register rather than a stale record). Section 5.6 fixed the coverage screen (JS-16's first screen) ending in prose instead of real numbers, four times over. Section 5.7 fixed the same defect as 5.5 one screen over, on the candidate money shelf, plus a route-level race (register read after store instead of before) that could publish a superseded figure as settled."
  ],
  "files_touched": [
    "kairos_api/model_console_api.py",
    "tv-break-dashboard/src/model/console/ModelConsole.jsx",
    "tv-break-dashboard/src/model/console/TrainingPanel.jsx",
    "tv-break-dashboard/src/model/console/console-bits.jsx",
    "tv-break-dashboard/src/model/console/console-words.js",
    "tv-break-dashboard/src/model/console/model-console.css",
    "tv-break-dashboard/src/model/console/model-console-panels.css",
    "tests/test_p7_can_edit.py",
    "docs/ux-gauntlet/contracts/P7.md",
    "docs/ux-gauntlet/state/P7.json"
  ]
}
