P12 round 2. The verdict act at the terminal, three states. Nothing here was recorded: no run below carries --perform.

=== 1. planned, every check passes, nothing written ===
Verdict for afterwindow: not_shipped
  [pass] candidate_exists         The candidate afterwindow is in models/candidates/.
  [pass] decision_is_a_verdict    The verdict is not_shipped.
  [pass] rescore_current          The held-out re-score is current, so this verdict rests on every artifact scored on one common set of breaks.
  [pass] actor_named              The steward taking this verdict is named.
  [pass] reason_given             The verdict carries its reason.

Money carried into the record: +923,843.08 on the operator's own channel over 2540 rows, direction up
  basis: the weekly plan the run path computes, summed over the operator's own channel
Re-score verdict carried into the record: not_distinguishable
  Taken on the common-basis re-score: every artifact scored on one identical set of breaks with one metric, not on each artifact's own self-reported held-out figures, which come from different splits and are not comparable.

Every check passed and nothing has been written.
Add --perform to record it.
outcome: ready

=== 2. a ship verdict with no release note and a stale money figure ===
Verdict for placebo_corrected: shipped
  [pass] candidate_exists         The candidate placebo_corrected is in models/candidates/.
  [pass] decision_is_a_verdict    The verdict is shipped.
  [pass] rescore_current          The held-out re-score is current, so this verdict rests on every artifact scored on one common set of breaks.
  [pass] actor_named              The steward taking this verdict is named.
  [pass] reason_given             The verdict carries its reason.
  [STOP] release_note_written     A ship verdict needs a release note in Hebrew, because it is the one training-side sentence an operator reads.
                                  --release-note-he "<sentence the operator side reads>"
  [STOP] money_measured           The stored money measurement is not current. What changed: settings.
                                  python scripts/adopt_candidate.py measure placebo_corrected

Money that would be carried, if this were not refused: stale, so the record states that rather than carrying a figure
Re-score verdict that would be carried, if this were not refused: identical

outcome: refused

=== 3. a candidate that does not exist ===
Verdict for nope: not_shipped
  [STOP] candidate_exists         There is no candidate called nope. Known: afterwindow, calibrated, competitor, placebo_corrected, spotclip.
  [pass] decision_is_a_verdict    The verdict is not_shipped.
  [STOP] rescore_current          This candidate is not in the stored re-score.
                                  python scripts/adopt_candidate.py rescore
  [pass] actor_named              The steward taking this verdict is named.
  [pass] reason_given             The verdict carries its reason.

Money that would be carried, if this were not refused: not measured, so the record states that rather than carrying a figure
Re-score verdict that would be carried, if this were not refused: unknown

outcome: refused
